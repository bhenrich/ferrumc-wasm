// @generated by ferrumc-proto-gen. Do not edit by hand.
// source: minecraft-data a8cf733fe44f3069f87f63e7ec3d74b521840ded (Minecraft 1.21.8, protocol 772, data version 4440)

//! Packets for the play state (Minecraft 1.21.8, protocol 772).

use bytes::BytesMut;

use ferrumc_codec::{BoundedReader, BoundedString};

use crate::error::ProtoError;
use crate::wire;
use crate::{Direction, State};

/// `ChunkBlockEntity`: a composite payload used on the wire.
#[derive(Debug, Clone, PartialEq)]
pub struct ChunkBlockEntity {
    packed_xz: u8,
    y: i16,
    block_entity_type: i32,
    data: ferrumc_nbt::NbtTag,
}

impl ChunkBlockEntity {
    /// Creates a new `ChunkBlockEntity` from its wire fields.
    pub fn new(packed_xz: u8, y: i16, block_entity_type: i32, data: ferrumc_nbt::NbtTag) -> Self {
        Self {
            packed_xz,
            y,
            block_entity_type,
            data,
        }
    }

    /// Returns the `packed_xz` field.
    pub fn packed_xz(&self) -> u8 {
        self.packed_xz
    }

    /// Returns the `y` field.
    pub fn y(&self) -> i16 {
        self.y
    }

    /// Returns the `block_entity_type` field.
    pub fn block_entity_type(&self) -> i32 {
        self.block_entity_type
    }

    /// Returns the `data` field.
    pub fn data(&self) -> &ferrumc_nbt::NbtTag {
        &self.data
    }

    /// Decodes a `ChunkBlockEntity` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let packed_xz = reader.read_u8()?;
        let y = reader.read_i16()?;
        let block_entity_type = reader.read_var_int()?;
        let data = {
            let available = reader.remaining();
            let bytes = reader.read_bytes(available)?;
            let (tag, consumed) = ferrumc_nbt::read_network_root_with_consumed(
                bytes,
                &ferrumc_nbt::NbtLimits::default(),
            )?;
            *reader = BoundedReader::new(bytes.get(consumed..).unwrap_or_default());
            tag
        };
        Ok(Self {
            packed_xz,
            y,
            block_entity_type,
            data,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        wire::write_u8(buf, self.packed_xz);
        wire::write_i16(buf, self.y);
        ferrumc_codec::write_var_int(buf, self.block_entity_type);
        {
            let nbt_bytes =
                ferrumc_nbt::write_network_root(&self.data, &ferrumc_nbt::NbtLimits::default())?;
            wire::write_raw(buf, &nbt_bytes);
        }
        Ok(())
    }
}

/// `CommandSuggestionMatch`: a composite payload used on the wire.
#[derive(Debug, Clone, PartialEq)]
pub struct CommandSuggestionMatch {
    suggestion: BoundedString<32_767>,
    tooltip: Option<ferrumc_nbt::NbtTag>,
}

impl CommandSuggestionMatch {
    /// Creates a new `CommandSuggestionMatch` from its wire fields.
    pub fn new(suggestion: BoundedString<32_767>, tooltip: Option<ferrumc_nbt::NbtTag>) -> Self {
        Self {
            suggestion,
            tooltip,
        }
    }

    /// Returns the `suggestion` field.
    pub fn suggestion(&self) -> &BoundedString<32_767> {
        &self.suggestion
    }

    /// Returns the `tooltip` field.
    pub fn tooltip(&self) -> Option<&ferrumc_nbt::NbtTag> {
        self.tooltip.as_ref()
    }

    /// Decodes a `CommandSuggestionMatch` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let suggestion = BoundedString::<32_767>::read(reader)?;
        let tooltip = if wire::read_bool(reader)? {
            Some({
                let available = reader.remaining();
                let bytes = reader.read_bytes(available)?;
                let (tag, consumed) = ferrumc_nbt::read_network_root_with_consumed(
                    bytes,
                    &ferrumc_nbt::NbtLimits::default(),
                )?;
                *reader = BoundedReader::new(bytes.get(consumed..).unwrap_or_default());
                tag
            })
        } else {
            None
        };
        Ok(Self {
            suggestion,
            tooltip,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        self.suggestion.write(buf);
        match &self.tooltip {
            Some(inner) => {
                wire::write_bool(buf, true);
                {
                    let nbt_bytes =
                        ferrumc_nbt::write_network_root(inner, &ferrumc_nbt::NbtLimits::default())?;
                    wire::write_raw(buf, &nbt_bytes);
                }
            }
            None => {
                wire::write_bool(buf, false);
            }
        }
        Ok(())
    }
}

/// `DeathLocation`: a composite payload used on the wire.
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct DeathLocation {
    dimension_name: BoundedString<32_767>,
    location: crate::types::BlockPosition,
}

impl DeathLocation {
    /// Creates a new `DeathLocation` from its wire fields.
    pub fn new(
        dimension_name: BoundedString<32_767>,
        location: crate::types::BlockPosition,
    ) -> Self {
        Self {
            dimension_name,
            location,
        }
    }

    /// Returns the `dimension_name` field.
    pub fn dimension_name(&self) -> &BoundedString<32_767> {
        &self.dimension_name
    }

    /// Returns the `location` field.
    pub fn location(&self) -> crate::types::BlockPosition {
        self.location
    }

    /// Decodes a `DeathLocation` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let dimension_name = BoundedString::<32_767>::read(reader)?;
        let location = crate::types::BlockPosition::read(reader)?;
        Ok(Self {
            dimension_name,
            location,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        self.dimension_name.write(buf);
        self.location.write(buf);
        Ok(())
    }
}

/// `EntityVelocity`: a composite payload used on the wire.
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct EntityVelocity {
    x: i16,
    y: i16,
    z: i16,
}

impl EntityVelocity {
    /// Creates a new `EntityVelocity` from its wire fields.
    pub fn new(x: i16, y: i16, z: i16) -> Self {
        Self { x, y, z }
    }

    /// Returns the `x` field.
    pub fn x(&self) -> i16 {
        self.x
    }

    /// Returns the `y` field.
    pub fn y(&self) -> i16 {
        self.y
    }

    /// Returns the `z` field.
    pub fn z(&self) -> i16 {
        self.z
    }

    /// Decodes a `EntityVelocity` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let x = reader.read_i16()?;
        let y = reader.read_i16()?;
        let z = reader.read_i16()?;
        Ok(Self { x, y, z })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        wire::write_i16(buf, self.x);
        wire::write_i16(buf, self.y);
        wire::write_i16(buf, self.z);
        Ok(())
    }
}

/// `Heightmap`: a composite payload used on the wire.
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct Heightmap {
    kind: i32,
    data: Vec<i64>,
}

impl Heightmap {
    /// Creates a new `Heightmap` from its wire fields.
    pub fn new(kind: i32, data: Vec<i64>) -> Self {
        Self { kind, data }
    }

    /// Returns the `kind` field.
    pub fn kind(&self) -> i32 {
        self.kind
    }

    /// Returns the `data` field.
    pub fn data(&self) -> &[i64] {
        &self.data
    }

    /// Decodes a `Heightmap` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let kind = reader.read_var_int()?;
        let data = {
            let count = wire::read_prefixed_len(reader)?;
            let mut items = Vec::with_capacity(count.min(reader.remaining()));
            for _ in 0..count {
                items.push(reader.read_i64()?);
            }
            items
        };
        Ok(Self { kind, data })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, self.kind);
        wire::write_prefixed_len(buf, self.data.len());
        for elem in &self.data {
            wire::write_i64(buf, *elem);
        }
        Ok(())
    }
}

/// `SpawnInfo`: a composite payload used on the wire.
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct SpawnInfo {
    dimension_type: i32,
    dimension_name: BoundedString<32_767>,
    hashed_seed: i64,
    gamemode: u8,
    previous_gamemode: u8,
    is_debug: bool,
    is_flat: bool,
    death_location: Option<DeathLocation>,
    portal_cooldown: i32,
    sea_level: i32,
}

impl SpawnInfo {
    /// Creates a new `SpawnInfo` from its wire fields.
    #[allow(
        clippy::too_many_arguments,
        reason = "mirrors the packet's wire fields"
    )]
    pub fn new(
        dimension_type: i32,
        dimension_name: BoundedString<32_767>,
        hashed_seed: i64,
        gamemode: u8,
        previous_gamemode: u8,
        is_debug: bool,
        is_flat: bool,
        death_location: Option<DeathLocation>,
        portal_cooldown: i32,
        sea_level: i32,
    ) -> Self {
        Self {
            dimension_type,
            dimension_name,
            hashed_seed,
            gamemode,
            previous_gamemode,
            is_debug,
            is_flat,
            death_location,
            portal_cooldown,
            sea_level,
        }
    }

    /// Returns the `dimension_type` field.
    pub fn dimension_type(&self) -> i32 {
        self.dimension_type
    }

    /// Returns the `dimension_name` field.
    pub fn dimension_name(&self) -> &BoundedString<32_767> {
        &self.dimension_name
    }

    /// Returns the `hashed_seed` field.
    pub fn hashed_seed(&self) -> i64 {
        self.hashed_seed
    }

    /// Returns the `gamemode` field.
    pub fn gamemode(&self) -> u8 {
        self.gamemode
    }

    /// Returns the `previous_gamemode` field.
    pub fn previous_gamemode(&self) -> u8 {
        self.previous_gamemode
    }

    /// Returns the `is_debug` field.
    pub fn is_debug(&self) -> bool {
        self.is_debug
    }

    /// Returns the `is_flat` field.
    pub fn is_flat(&self) -> bool {
        self.is_flat
    }

    /// Returns the `death_location` field.
    pub fn death_location(&self) -> Option<&DeathLocation> {
        self.death_location.as_ref()
    }

    /// Returns the `portal_cooldown` field.
    pub fn portal_cooldown(&self) -> i32 {
        self.portal_cooldown
    }

    /// Returns the `sea_level` field.
    pub fn sea_level(&self) -> i32 {
        self.sea_level
    }

    /// Decodes a `SpawnInfo` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let dimension_type = reader.read_var_int()?;
        let dimension_name = BoundedString::<32_767>::read(reader)?;
        let hashed_seed = reader.read_i64()?;
        let gamemode = reader.read_u8()?;
        let previous_gamemode = reader.read_u8()?;
        let is_debug = wire::read_bool(reader)?;
        let is_flat = wire::read_bool(reader)?;
        let death_location = if wire::read_bool(reader)? {
            Some(DeathLocation::decode(reader)?)
        } else {
            None
        };
        let portal_cooldown = reader.read_var_int()?;
        let sea_level = reader.read_var_int()?;
        Ok(Self {
            dimension_type,
            dimension_name,
            hashed_seed,
            gamemode,
            previous_gamemode,
            is_debug,
            is_flat,
            death_location,
            portal_cooldown,
            sea_level,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, self.dimension_type);
        self.dimension_name.write(buf);
        wire::write_i64(buf, self.hashed_seed);
        wire::write_u8(buf, self.gamemode);
        wire::write_u8(buf, self.previous_gamemode);
        wire::write_bool(buf, self.is_debug);
        wire::write_bool(buf, self.is_flat);
        match &self.death_location {
            Some(inner) => {
                wire::write_bool(buf, true);
                inner.encode(buf)?;
            }
            None => {
                wire::write_bool(buf, false);
            }
        }
        ferrumc_codec::write_var_int(buf, self.portal_cooldown);
        ferrumc_codec::write_var_int(buf, self.sea_level);
        Ok(())
    }
}

/// `ConfirmTeleportation`: the play serverbound packet (wire id `0x00`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct ConfirmTeleportation {
    teleport_id: i32,
}

impl ConfirmTeleportation {
    /// The wire packet id for `ConfirmTeleportation`.
    pub const PACKET_ID: i32 = 0x00;

    /// Creates a new `ConfirmTeleportation` from its wire fields.
    pub fn new(teleport_id: i32) -> Self {
        Self { teleport_id }
    }

    /// Returns the `teleport_id` field.
    pub fn teleport_id(&self) -> i32 {
        self.teleport_id
    }

    /// Decodes a `ConfirmTeleportation` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let teleport_id = reader.read_var_int()?;
        Ok(Self { teleport_id })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        ferrumc_codec::write_var_int(buf, self.teleport_id);
        Ok(())
    }
}

/// `ChatCommand`: the play serverbound packet (wire id `0x06`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct ChatCommand {
    command: BoundedString<256>,
}

impl ChatCommand {
    /// The wire packet id for `ChatCommand`.
    pub const PACKET_ID: i32 = 0x06;

    /// Creates a new `ChatCommand` from its wire fields.
    pub fn new(command: BoundedString<256>) -> Self {
        Self { command }
    }

    /// Returns the `command` field.
    pub fn command(&self) -> &BoundedString<256> {
        &self.command
    }

    /// Decodes a `ChatCommand` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let command = BoundedString::<256>::read(reader)?;
        Ok(Self { command })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        self.command.write(buf);
        Ok(())
    }
}

/// `ChatMessage`: the play serverbound packet (wire id `0x08`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct ChatMessage {
    message: BoundedString<256>,
    rest: Vec<u8>,
}

impl ChatMessage {
    /// The wire packet id for `ChatMessage`.
    pub const PACKET_ID: i32 = 0x08;

    /// Creates a new `ChatMessage` from its wire fields.
    pub fn new(message: BoundedString<256>, rest: Vec<u8>) -> Self {
        Self { message, rest }
    }

    /// Returns the `message` field.
    pub fn message(&self) -> &BoundedString<256> {
        &self.message
    }

    /// Returns the `rest` field.
    pub fn rest(&self) -> &[u8] {
        &self.rest
    }

    /// Decodes a `ChatMessage` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let message = BoundedString::<256>::read(reader)?;
        let rest = {
            let remaining = reader.remaining();
            reader.read_bytes(remaining)?.to_vec()
        };
        Ok(Self { message, rest })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        self.message.write(buf);
        wire::write_raw(buf, &self.rest);
        Ok(())
    }
}

/// `TabCompleteRequest`: the play serverbound packet (wire id `0x0e`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct TabCompleteRequest {
    transaction_id: i32,
    text: BoundedString<32_767>,
}

impl TabCompleteRequest {
    /// The wire packet id for `TabCompleteRequest`.
    pub const PACKET_ID: i32 = 0x0e;

    /// Creates a new `TabCompleteRequest` from its wire fields.
    pub fn new(transaction_id: i32, text: BoundedString<32_767>) -> Self {
        Self {
            transaction_id,
            text,
        }
    }

    /// Returns the `transaction_id` field.
    pub fn transaction_id(&self) -> i32 {
        self.transaction_id
    }

    /// Returns the `text` field.
    pub fn text(&self) -> &BoundedString<32_767> {
        &self.text
    }

    /// Decodes a `TabCompleteRequest` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let transaction_id = reader.read_var_int()?;
        let text = BoundedString::<32_767>::read(reader)?;
        Ok(Self {
            transaction_id,
            text,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        ferrumc_codec::write_var_int(buf, self.transaction_id);
        self.text.write(buf);
        Ok(())
    }
}

/// `WindowClick`: the play serverbound packet (wire id `0x11`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct WindowClick {
    window_id: i32,
    state_id: i32,
    rest: Vec<u8>,
}

impl WindowClick {
    /// The wire packet id for `WindowClick`.
    pub const PACKET_ID: i32 = 0x11;

    /// Creates a new `WindowClick` from its wire fields.
    pub fn new(window_id: i32, state_id: i32, rest: Vec<u8>) -> Self {
        Self {
            window_id,
            state_id,
            rest,
        }
    }

    /// Returns the `window_id` field.
    pub fn window_id(&self) -> i32 {
        self.window_id
    }

    /// Returns the `state_id` field.
    pub fn state_id(&self) -> i32 {
        self.state_id
    }

    /// Returns the `rest` field.
    pub fn rest(&self) -> &[u8] {
        &self.rest
    }

    /// Decodes a `WindowClick` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let window_id = reader.read_var_int()?;
        let state_id = reader.read_var_int()?;
        let rest = {
            let remaining = reader.remaining();
            reader.read_bytes(remaining)?.to_vec()
        };
        Ok(Self {
            window_id,
            state_id,
            rest,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        ferrumc_codec::write_var_int(buf, self.window_id);
        ferrumc_codec::write_var_int(buf, self.state_id);
        wire::write_raw(buf, &self.rest);
        Ok(())
    }
}

/// `ServerboundKeepAlive`: the play serverbound packet (wire id `0x1b`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct ServerboundKeepAlive {
    keep_alive_id: i64,
}

impl ServerboundKeepAlive {
    /// The wire packet id for `ServerboundKeepAlive`.
    pub const PACKET_ID: i32 = 0x1b;

    /// Creates a new `ServerboundKeepAlive` from its wire fields.
    pub fn new(keep_alive_id: i64) -> Self {
        Self { keep_alive_id }
    }

    /// Returns the `keep_alive_id` field.
    pub fn keep_alive_id(&self) -> i64 {
        self.keep_alive_id
    }

    /// Decodes a `ServerboundKeepAlive` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let keep_alive_id = reader.read_i64()?;
        Ok(Self { keep_alive_id })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        wire::write_i64(buf, self.keep_alive_id);
        Ok(())
    }
}

/// `SetPlayerPosition`: the play serverbound packet (wire id `0x1d`).
#[derive(Debug, Clone, PartialEq)]
pub struct SetPlayerPosition {
    x: f64,
    y: f64,
    z: f64,
    flags: u8,
}

impl SetPlayerPosition {
    /// The wire packet id for `SetPlayerPosition`.
    pub const PACKET_ID: i32 = 0x1d;

    /// Creates a new `SetPlayerPosition` from its wire fields.
    pub fn new(x: f64, y: f64, z: f64, flags: u8) -> Self {
        Self { x, y, z, flags }
    }

    /// Returns the `x` field.
    pub fn x(&self) -> f64 {
        self.x
    }

    /// Returns the `y` field.
    pub fn y(&self) -> f64 {
        self.y
    }

    /// Returns the `z` field.
    pub fn z(&self) -> f64 {
        self.z
    }

    /// Returns the `flags` field.
    pub fn flags(&self) -> u8 {
        self.flags
    }

    /// Decodes a `SetPlayerPosition` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let x = reader.read_f64()?;
        let y = reader.read_f64()?;
        let z = reader.read_f64()?;
        let flags = reader.read_u8()?;
        Ok(Self { x, y, z, flags })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        wire::write_f64(buf, self.x);
        wire::write_f64(buf, self.y);
        wire::write_f64(buf, self.z);
        wire::write_u8(buf, self.flags);
        Ok(())
    }
}

/// `SetPlayerPositionAndRotation`: the play serverbound packet (wire id `0x1e`).
#[derive(Debug, Clone, PartialEq)]
pub struct SetPlayerPositionAndRotation {
    x: f64,
    y: f64,
    z: f64,
    yaw: f32,
    pitch: f32,
    flags: u8,
}

impl SetPlayerPositionAndRotation {
    /// The wire packet id for `SetPlayerPositionAndRotation`.
    pub const PACKET_ID: i32 = 0x1e;

    /// Creates a new `SetPlayerPositionAndRotation` from its wire fields.
    pub fn new(x: f64, y: f64, z: f64, yaw: f32, pitch: f32, flags: u8) -> Self {
        Self {
            x,
            y,
            z,
            yaw,
            pitch,
            flags,
        }
    }

    /// Returns the `x` field.
    pub fn x(&self) -> f64 {
        self.x
    }

    /// Returns the `y` field.
    pub fn y(&self) -> f64 {
        self.y
    }

    /// Returns the `z` field.
    pub fn z(&self) -> f64 {
        self.z
    }

    /// Returns the `yaw` field.
    pub fn yaw(&self) -> f32 {
        self.yaw
    }

    /// Returns the `pitch` field.
    pub fn pitch(&self) -> f32 {
        self.pitch
    }

    /// Returns the `flags` field.
    pub fn flags(&self) -> u8 {
        self.flags
    }

    /// Decodes a `SetPlayerPositionAndRotation` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let x = reader.read_f64()?;
        let y = reader.read_f64()?;
        let z = reader.read_f64()?;
        let yaw = reader.read_f32()?;
        let pitch = reader.read_f32()?;
        let flags = reader.read_u8()?;
        Ok(Self {
            x,
            y,
            z,
            yaw,
            pitch,
            flags,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        wire::write_f64(buf, self.x);
        wire::write_f64(buf, self.y);
        wire::write_f64(buf, self.z);
        wire::write_f32(buf, self.yaw);
        wire::write_f32(buf, self.pitch);
        wire::write_u8(buf, self.flags);
        Ok(())
    }
}

/// `SetPlayerRotation`: the play serverbound packet (wire id `0x1f`).
#[derive(Debug, Clone, PartialEq)]
pub struct SetPlayerRotation {
    yaw: f32,
    pitch: f32,
    flags: u8,
}

impl SetPlayerRotation {
    /// The wire packet id for `SetPlayerRotation`.
    pub const PACKET_ID: i32 = 0x1f;

    /// Creates a new `SetPlayerRotation` from its wire fields.
    pub fn new(yaw: f32, pitch: f32, flags: u8) -> Self {
        Self { yaw, pitch, flags }
    }

    /// Returns the `yaw` field.
    pub fn yaw(&self) -> f32 {
        self.yaw
    }

    /// Returns the `pitch` field.
    pub fn pitch(&self) -> f32 {
        self.pitch
    }

    /// Returns the `flags` field.
    pub fn flags(&self) -> u8 {
        self.flags
    }

    /// Decodes a `SetPlayerRotation` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let yaw = reader.read_f32()?;
        let pitch = reader.read_f32()?;
        let flags = reader.read_u8()?;
        Ok(Self { yaw, pitch, flags })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        wire::write_f32(buf, self.yaw);
        wire::write_f32(buf, self.pitch);
        wire::write_u8(buf, self.flags);
        Ok(())
    }
}

/// `PlayerAction`: the play serverbound packet (wire id `0x28`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct PlayerAction {
    status: i32,
    location: crate::types::BlockPosition,
    face: i8,
    sequence: i32,
}

impl PlayerAction {
    /// The wire packet id for `PlayerAction`.
    pub const PACKET_ID: i32 = 0x28;

    /// Creates a new `PlayerAction` from its wire fields.
    pub fn new(
        status: i32,
        location: crate::types::BlockPosition,
        face: i8,
        sequence: i32,
    ) -> Self {
        Self {
            status,
            location,
            face,
            sequence,
        }
    }

    /// Returns the `status` field.
    pub fn status(&self) -> i32 {
        self.status
    }

    /// Returns the `location` field.
    pub fn location(&self) -> crate::types::BlockPosition {
        self.location
    }

    /// Returns the `face` field.
    pub fn face(&self) -> i8 {
        self.face
    }

    /// Returns the `sequence` field.
    pub fn sequence(&self) -> i32 {
        self.sequence
    }

    /// Decodes a `PlayerAction` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let status = reader.read_var_int()?;
        let location = crate::types::BlockPosition::read(reader)?;
        let face = reader.read_i8()?;
        let sequence = reader.read_var_int()?;
        Ok(Self {
            status,
            location,
            face,
            sequence,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        ferrumc_codec::write_var_int(buf, self.status);
        self.location.write(buf);
        wire::write_i8(buf, self.face);
        ferrumc_codec::write_var_int(buf, self.sequence);
        Ok(())
    }
}

/// `ServerboundSetHeldItem`: the play serverbound packet (wire id `0x34`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct ServerboundSetHeldItem {
    slot: i16,
}

impl ServerboundSetHeldItem {
    /// The wire packet id for `ServerboundSetHeldItem`.
    pub const PACKET_ID: i32 = 0x34;

    /// Creates a new `ServerboundSetHeldItem` from its wire fields.
    pub fn new(slot: i16) -> Self {
        Self { slot }
    }

    /// Returns the `slot` field.
    pub fn slot(&self) -> i16 {
        self.slot
    }

    /// Decodes a `ServerboundSetHeldItem` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let slot = reader.read_i16()?;
        Ok(Self { slot })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        wire::write_i16(buf, self.slot);
        Ok(())
    }
}

/// `SetCreativeSlot`: the play serverbound packet (wire id `0x37`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct SetCreativeSlot {
    slot: i16,
    item: Vec<u8>,
}

impl SetCreativeSlot {
    /// The wire packet id for `SetCreativeSlot`.
    pub const PACKET_ID: i32 = 0x37;

    /// Creates a new `SetCreativeSlot` from its wire fields.
    pub fn new(slot: i16, item: Vec<u8>) -> Self {
        Self { slot, item }
    }

    /// Returns the `slot` field.
    pub fn slot(&self) -> i16 {
        self.slot
    }

    /// Returns the `item` field.
    pub fn item(&self) -> &[u8] {
        &self.item
    }

    /// Decodes a `SetCreativeSlot` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let slot = reader.read_i16()?;
        let item = {
            let remaining = reader.remaining();
            reader.read_bytes(remaining)?.to_vec()
        };
        Ok(Self { slot, item })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        wire::write_i16(buf, self.slot);
        wire::write_raw(buf, &self.item);
        Ok(())
    }
}

/// `UpdateSign`: the play serverbound packet (wire id `0x3b`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct UpdateSign {
    location: crate::types::BlockPosition,
    is_front_text: bool,
    line_1: BoundedString<384>,
    line_2: BoundedString<384>,
    line_3: BoundedString<384>,
    line_4: BoundedString<384>,
}

impl UpdateSign {
    /// The wire packet id for `UpdateSign`.
    pub const PACKET_ID: i32 = 0x3b;

    /// Creates a new `UpdateSign` from its wire fields.
    pub fn new(
        location: crate::types::BlockPosition,
        is_front_text: bool,
        line_1: BoundedString<384>,
        line_2: BoundedString<384>,
        line_3: BoundedString<384>,
        line_4: BoundedString<384>,
    ) -> Self {
        Self {
            location,
            is_front_text,
            line_1,
            line_2,
            line_3,
            line_4,
        }
    }

    /// Returns the `location` field.
    pub fn location(&self) -> crate::types::BlockPosition {
        self.location
    }

    /// Returns the `is_front_text` field.
    pub fn is_front_text(&self) -> bool {
        self.is_front_text
    }

    /// Returns the `line_1` field.
    pub fn line_1(&self) -> &BoundedString<384> {
        &self.line_1
    }

    /// Returns the `line_2` field.
    pub fn line_2(&self) -> &BoundedString<384> {
        &self.line_2
    }

    /// Returns the `line_3` field.
    pub fn line_3(&self) -> &BoundedString<384> {
        &self.line_3
    }

    /// Returns the `line_4` field.
    pub fn line_4(&self) -> &BoundedString<384> {
        &self.line_4
    }

    /// Decodes a `UpdateSign` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let location = crate::types::BlockPosition::read(reader)?;
        let is_front_text = wire::read_bool(reader)?;
        let line_1 = BoundedString::<384>::read(reader)?;
        let line_2 = BoundedString::<384>::read(reader)?;
        let line_3 = BoundedString::<384>::read(reader)?;
        let line_4 = BoundedString::<384>::read(reader)?;
        Ok(Self {
            location,
            is_front_text,
            line_1,
            line_2,
            line_3,
            line_4,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        self.location.write(buf);
        wire::write_bool(buf, self.is_front_text);
        self.line_1.write(buf);
        self.line_2.write(buf);
        self.line_3.write(buf);
        self.line_4.write(buf);
        Ok(())
    }
}

/// `UseItemOn`: the play serverbound packet (wire id `0x3f`).
#[derive(Debug, Clone, PartialEq)]
pub struct UseItemOn {
    hand: i32,
    location: crate::types::BlockPosition,
    direction: i32,
    cursor_x: f32,
    cursor_y: f32,
    cursor_z: f32,
    inside_block: bool,
    world_border_hit: bool,
    sequence: i32,
}

impl UseItemOn {
    /// The wire packet id for `UseItemOn`.
    pub const PACKET_ID: i32 = 0x3f;

    /// Creates a new `UseItemOn` from its wire fields.
    #[allow(
        clippy::too_many_arguments,
        reason = "mirrors the packet's wire fields"
    )]
    pub fn new(
        hand: i32,
        location: crate::types::BlockPosition,
        direction: i32,
        cursor_x: f32,
        cursor_y: f32,
        cursor_z: f32,
        inside_block: bool,
        world_border_hit: bool,
        sequence: i32,
    ) -> Self {
        Self {
            hand,
            location,
            direction,
            cursor_x,
            cursor_y,
            cursor_z,
            inside_block,
            world_border_hit,
            sequence,
        }
    }

    /// Returns the `hand` field.
    pub fn hand(&self) -> i32 {
        self.hand
    }

    /// Returns the `location` field.
    pub fn location(&self) -> crate::types::BlockPosition {
        self.location
    }

    /// Returns the `direction` field.
    pub fn direction(&self) -> i32 {
        self.direction
    }

    /// Returns the `cursor_x` field.
    pub fn cursor_x(&self) -> f32 {
        self.cursor_x
    }

    /// Returns the `cursor_y` field.
    pub fn cursor_y(&self) -> f32 {
        self.cursor_y
    }

    /// Returns the `cursor_z` field.
    pub fn cursor_z(&self) -> f32 {
        self.cursor_z
    }

    /// Returns the `inside_block` field.
    pub fn inside_block(&self) -> bool {
        self.inside_block
    }

    /// Returns the `world_border_hit` field.
    pub fn world_border_hit(&self) -> bool {
        self.world_border_hit
    }

    /// Returns the `sequence` field.
    pub fn sequence(&self) -> i32 {
        self.sequence
    }

    /// Decodes a `UseItemOn` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let hand = reader.read_var_int()?;
        let location = crate::types::BlockPosition::read(reader)?;
        let direction = reader.read_var_int()?;
        let cursor_x = reader.read_f32()?;
        let cursor_y = reader.read_f32()?;
        let cursor_z = reader.read_f32()?;
        let inside_block = wire::read_bool(reader)?;
        let world_border_hit = wire::read_bool(reader)?;
        let sequence = reader.read_var_int()?;
        Ok(Self {
            hand,
            location,
            direction,
            cursor_x,
            cursor_y,
            cursor_z,
            inside_block,
            world_border_hit,
            sequence,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        ferrumc_codec::write_var_int(buf, self.hand);
        self.location.write(buf);
        ferrumc_codec::write_var_int(buf, self.direction);
        wire::write_f32(buf, self.cursor_x);
        wire::write_f32(buf, self.cursor_y);
        wire::write_f32(buf, self.cursor_z);
        wire::write_bool(buf, self.inside_block);
        wire::write_bool(buf, self.world_border_hit);
        ferrumc_codec::write_var_int(buf, self.sequence);
        Ok(())
    }
}

/// `SpawnEntity`: the play clientbound packet (wire id `0x01`).
#[derive(Debug, Clone, PartialEq)]
pub struct SpawnEntity {
    entity_id: i32,
    entity_uuid: uuid::Uuid,
    entity_type: i32,
    x: f64,
    y: f64,
    z: f64,
    pitch: i8,
    yaw: i8,
    head_pitch: i8,
    data: i32,
    velocity: EntityVelocity,
}

impl SpawnEntity {
    /// The wire packet id for `SpawnEntity`.
    pub const PACKET_ID: i32 = 0x01;

    /// Creates a new `SpawnEntity` from its wire fields.
    #[allow(
        clippy::too_many_arguments,
        reason = "mirrors the packet's wire fields"
    )]
    pub fn new(
        entity_id: i32,
        entity_uuid: uuid::Uuid,
        entity_type: i32,
        x: f64,
        y: f64,
        z: f64,
        pitch: i8,
        yaw: i8,
        head_pitch: i8,
        data: i32,
        velocity: EntityVelocity,
    ) -> Self {
        Self {
            entity_id,
            entity_uuid,
            entity_type,
            x,
            y,
            z,
            pitch,
            yaw,
            head_pitch,
            data,
            velocity,
        }
    }

    /// Returns the `entity_id` field.
    pub fn entity_id(&self) -> i32 {
        self.entity_id
    }

    /// Returns the `entity_uuid` field.
    pub fn entity_uuid(&self) -> uuid::Uuid {
        self.entity_uuid
    }

    /// Returns the `entity_type` field.
    pub fn entity_type(&self) -> i32 {
        self.entity_type
    }

    /// Returns the `x` field.
    pub fn x(&self) -> f64 {
        self.x
    }

    /// Returns the `y` field.
    pub fn y(&self) -> f64 {
        self.y
    }

    /// Returns the `z` field.
    pub fn z(&self) -> f64 {
        self.z
    }

    /// Returns the `pitch` field.
    pub fn pitch(&self) -> i8 {
        self.pitch
    }

    /// Returns the `yaw` field.
    pub fn yaw(&self) -> i8 {
        self.yaw
    }

    /// Returns the `head_pitch` field.
    pub fn head_pitch(&self) -> i8 {
        self.head_pitch
    }

    /// Returns the `data` field.
    pub fn data(&self) -> i32 {
        self.data
    }

    /// Returns the `velocity` field.
    pub fn velocity(&self) -> &EntityVelocity {
        &self.velocity
    }

    /// Decodes a `SpawnEntity` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let entity_id = reader.read_var_int()?;
        let entity_uuid = wire::read_uuid(reader)?;
        let entity_type = reader.read_var_int()?;
        let x = reader.read_f64()?;
        let y = reader.read_f64()?;
        let z = reader.read_f64()?;
        let pitch = reader.read_i8()?;
        let yaw = reader.read_i8()?;
        let head_pitch = reader.read_i8()?;
        let data = reader.read_var_int()?;
        let velocity = EntityVelocity::decode(reader)?;
        Ok(Self {
            entity_id,
            entity_uuid,
            entity_type,
            x,
            y,
            z,
            pitch,
            yaw,
            head_pitch,
            data,
            velocity,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        ferrumc_codec::write_var_int(buf, self.entity_id);
        wire::write_uuid(buf, self.entity_uuid);
        ferrumc_codec::write_var_int(buf, self.entity_type);
        wire::write_f64(buf, self.x);
        wire::write_f64(buf, self.y);
        wire::write_f64(buf, self.z);
        wire::write_i8(buf, self.pitch);
        wire::write_i8(buf, self.yaw);
        wire::write_i8(buf, self.head_pitch);
        ferrumc_codec::write_var_int(buf, self.data);
        self.velocity.encode(buf)?;
        Ok(())
    }
}

/// `AcknowledgeBlockChange`: the play clientbound packet (wire id `0x04`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct AcknowledgeBlockChange {
    sequence: i32,
}

impl AcknowledgeBlockChange {
    /// The wire packet id for `AcknowledgeBlockChange`.
    pub const PACKET_ID: i32 = 0x04;

    /// Creates a new `AcknowledgeBlockChange` from its wire fields.
    pub fn new(sequence: i32) -> Self {
        Self { sequence }
    }

    /// Returns the `sequence` field.
    pub fn sequence(&self) -> i32 {
        self.sequence
    }

    /// Decodes a `AcknowledgeBlockChange` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let sequence = reader.read_var_int()?;
        Ok(Self { sequence })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        ferrumc_codec::write_var_int(buf, self.sequence);
        Ok(())
    }
}

/// `BlockEntityData`: the play clientbound packet (wire id `0x06`).
#[derive(Debug, Clone, PartialEq)]
pub struct BlockEntityData {
    location: crate::types::BlockPosition,
    block_entity_type: i32,
    data: ferrumc_nbt::NbtTag,
}

impl BlockEntityData {
    /// The wire packet id for `BlockEntityData`.
    pub const PACKET_ID: i32 = 0x06;

    /// Creates a new `BlockEntityData` from its wire fields.
    pub fn new(
        location: crate::types::BlockPosition,
        block_entity_type: i32,
        data: ferrumc_nbt::NbtTag,
    ) -> Self {
        Self {
            location,
            block_entity_type,
            data,
        }
    }

    /// Returns the `location` field.
    pub fn location(&self) -> crate::types::BlockPosition {
        self.location
    }

    /// Returns the `block_entity_type` field.
    pub fn block_entity_type(&self) -> i32 {
        self.block_entity_type
    }

    /// Returns the `data` field.
    pub fn data(&self) -> &ferrumc_nbt::NbtTag {
        &self.data
    }

    /// Decodes a `BlockEntityData` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let location = crate::types::BlockPosition::read(reader)?;
        let block_entity_type = reader.read_var_int()?;
        let data = {
            let available = reader.remaining();
            let bytes = reader.read_bytes(available)?;
            let (tag, consumed) = ferrumc_nbt::read_network_root_with_consumed(
                bytes,
                &ferrumc_nbt::NbtLimits::default(),
            )?;
            *reader = BoundedReader::new(bytes.get(consumed..).unwrap_or_default());
            tag
        };
        Ok(Self {
            location,
            block_entity_type,
            data,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        self.location.write(buf);
        ferrumc_codec::write_var_int(buf, self.block_entity_type);
        {
            let nbt_bytes =
                ferrumc_nbt::write_network_root(&self.data, &ferrumc_nbt::NbtLimits::default())?;
            wire::write_raw(buf, &nbt_bytes);
        }
        Ok(())
    }
}

/// `BlockUpdate`: the play clientbound packet (wire id `0x08`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct BlockUpdate {
    location: crate::types::BlockPosition,
    block_state: i32,
}

impl BlockUpdate {
    /// The wire packet id for `BlockUpdate`.
    pub const PACKET_ID: i32 = 0x08;

    /// Creates a new `BlockUpdate` from its wire fields.
    pub fn new(location: crate::types::BlockPosition, block_state: i32) -> Self {
        Self {
            location,
            block_state,
        }
    }

    /// Returns the `location` field.
    pub fn location(&self) -> crate::types::BlockPosition {
        self.location
    }

    /// Returns the `block_state` field.
    pub fn block_state(&self) -> i32 {
        self.block_state
    }

    /// Decodes a `BlockUpdate` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let location = crate::types::BlockPosition::read(reader)?;
        let block_state = reader.read_var_int()?;
        Ok(Self {
            location,
            block_state,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        self.location.write(buf);
        ferrumc_codec::write_var_int(buf, self.block_state);
        Ok(())
    }
}

/// `BossBar`: the play clientbound packet (wire id `0x09`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct BossBar {
    uuid: uuid::Uuid,
    action: i32,
    rest: Vec<u8>,
}

impl BossBar {
    /// The wire packet id for `BossBar`.
    pub const PACKET_ID: i32 = 0x09;

    /// Creates a new `BossBar` from its wire fields.
    pub fn new(uuid: uuid::Uuid, action: i32, rest: Vec<u8>) -> Self {
        Self { uuid, action, rest }
    }

    /// Returns the `uuid` field.
    pub fn uuid(&self) -> uuid::Uuid {
        self.uuid
    }

    /// Returns the `action` field.
    pub fn action(&self) -> i32 {
        self.action
    }

    /// Returns the `rest` field.
    pub fn rest(&self) -> &[u8] {
        &self.rest
    }

    /// Decodes a `BossBar` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let uuid = wire::read_uuid(reader)?;
        let action = reader.read_var_int()?;
        let rest = {
            let remaining = reader.remaining();
            reader.read_bytes(remaining)?.to_vec()
        };
        Ok(Self { uuid, action, rest })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        wire::write_uuid(buf, self.uuid);
        ferrumc_codec::write_var_int(buf, self.action);
        wire::write_raw(buf, &self.rest);
        Ok(())
    }
}

/// `TabCompleteResponse`: the play clientbound packet (wire id `0x0f`).
#[derive(Debug, Clone, PartialEq)]
pub struct TabCompleteResponse {
    transaction_id: i32,
    start: i32,
    length: i32,
    matches: Vec<CommandSuggestionMatch>,
}

impl TabCompleteResponse {
    /// The wire packet id for `TabCompleteResponse`.
    pub const PACKET_ID: i32 = 0x0f;

    /// Creates a new `TabCompleteResponse` from its wire fields.
    pub fn new(
        transaction_id: i32,
        start: i32,
        length: i32,
        matches: Vec<CommandSuggestionMatch>,
    ) -> Self {
        Self {
            transaction_id,
            start,
            length,
            matches,
        }
    }

    /// Returns the `transaction_id` field.
    pub fn transaction_id(&self) -> i32 {
        self.transaction_id
    }

    /// Returns the `start` field.
    pub fn start(&self) -> i32 {
        self.start
    }

    /// Returns the `length` field.
    pub fn length(&self) -> i32 {
        self.length
    }

    /// Returns the `matches` field.
    pub fn matches(&self) -> &[CommandSuggestionMatch] {
        &self.matches
    }

    /// Decodes a `TabCompleteResponse` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let transaction_id = reader.read_var_int()?;
        let start = reader.read_var_int()?;
        let length = reader.read_var_int()?;
        let matches = {
            let count = wire::read_prefixed_len(reader)?;
            let mut items = Vec::with_capacity(count.min(reader.remaining()));
            for _ in 0..count {
                items.push(CommandSuggestionMatch::decode(reader)?);
            }
            items
        };
        Ok(Self {
            transaction_id,
            start,
            length,
            matches,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        ferrumc_codec::write_var_int(buf, self.transaction_id);
        ferrumc_codec::write_var_int(buf, self.start);
        ferrumc_codec::write_var_int(buf, self.length);
        wire::write_prefixed_len(buf, self.matches.len());
        for elem in &self.matches {
            elem.encode(buf)?;
        }
        Ok(())
    }
}

/// `Commands`: the play clientbound packet (wire id `0x10`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct Commands {
    payload: Vec<u8>,
}

impl Commands {
    /// The wire packet id for `Commands`.
    pub const PACKET_ID: i32 = 0x10;

    /// Creates a new `Commands` from its wire fields.
    pub fn new(payload: Vec<u8>) -> Self {
        Self { payload }
    }

    /// Returns the `payload` field.
    pub fn payload(&self) -> &[u8] {
        &self.payload
    }

    /// Decodes a `Commands` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let payload = {
            let remaining = reader.remaining();
            reader.read_bytes(remaining)?.to_vec()
        };
        Ok(Self { payload })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        wire::write_raw(buf, &self.payload);
        Ok(())
    }
}

/// `SetContainerContent`: the play clientbound packet (wire id `0x12`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct SetContainerContent {
    window_id: i32,
    state_id: i32,
    payload: Vec<u8>,
}

impl SetContainerContent {
    /// The wire packet id for `SetContainerContent`.
    pub const PACKET_ID: i32 = 0x12;

    /// Creates a new `SetContainerContent` from its wire fields.
    pub fn new(window_id: i32, state_id: i32, payload: Vec<u8>) -> Self {
        Self {
            window_id,
            state_id,
            payload,
        }
    }

    /// Returns the `window_id` field.
    pub fn window_id(&self) -> i32 {
        self.window_id
    }

    /// Returns the `state_id` field.
    pub fn state_id(&self) -> i32 {
        self.state_id
    }

    /// Returns the `payload` field.
    pub fn payload(&self) -> &[u8] {
        &self.payload
    }

    /// Decodes a `SetContainerContent` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let window_id = reader.read_var_int()?;
        let state_id = reader.read_var_int()?;
        let payload = {
            let remaining = reader.remaining();
            reader.read_bytes(remaining)?.to_vec()
        };
        Ok(Self {
            window_id,
            state_id,
            payload,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        ferrumc_codec::write_var_int(buf, self.window_id);
        ferrumc_codec::write_var_int(buf, self.state_id);
        wire::write_raw(buf, &self.payload);
        Ok(())
    }
}

/// `SetContainerSlot`: the play clientbound packet (wire id `0x14`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct SetContainerSlot {
    window_id: i32,
    state_id: i32,
    slot: i16,
    item: Vec<u8>,
}

impl SetContainerSlot {
    /// The wire packet id for `SetContainerSlot`.
    pub const PACKET_ID: i32 = 0x14;

    /// Creates a new `SetContainerSlot` from its wire fields.
    pub fn new(window_id: i32, state_id: i32, slot: i16, item: Vec<u8>) -> Self {
        Self {
            window_id,
            state_id,
            slot,
            item,
        }
    }

    /// Returns the `window_id` field.
    pub fn window_id(&self) -> i32 {
        self.window_id
    }

    /// Returns the `state_id` field.
    pub fn state_id(&self) -> i32 {
        self.state_id
    }

    /// Returns the `slot` field.
    pub fn slot(&self) -> i16 {
        self.slot
    }

    /// Returns the `item` field.
    pub fn item(&self) -> &[u8] {
        &self.item
    }

    /// Decodes a `SetContainerSlot` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let window_id = reader.read_var_int()?;
        let state_id = reader.read_var_int()?;
        let slot = reader.read_i16()?;
        let item = {
            let remaining = reader.remaining();
            reader.read_bytes(remaining)?.to_vec()
        };
        Ok(Self {
            window_id,
            state_id,
            slot,
            item,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        ferrumc_codec::write_var_int(buf, self.window_id);
        ferrumc_codec::write_var_int(buf, self.state_id);
        wire::write_i16(buf, self.slot);
        wire::write_raw(buf, &self.item);
        Ok(())
    }
}

/// `EntityTeleport`: the play clientbound packet (wire id `0x1f`).
#[derive(Debug, Clone, PartialEq)]
pub struct EntityTeleport {
    entity_id: i32,
    x: f64,
    y: f64,
    z: f64,
    velocity_x: f64,
    velocity_y: f64,
    velocity_z: f64,
    yaw: f32,
    pitch: f32,
    on_ground: bool,
}

impl EntityTeleport {
    /// The wire packet id for `EntityTeleport`.
    pub const PACKET_ID: i32 = 0x1f;

    /// Creates a new `EntityTeleport` from its wire fields.
    #[allow(
        clippy::too_many_arguments,
        reason = "mirrors the packet's wire fields"
    )]
    pub fn new(
        entity_id: i32,
        x: f64,
        y: f64,
        z: f64,
        velocity_x: f64,
        velocity_y: f64,
        velocity_z: f64,
        yaw: f32,
        pitch: f32,
        on_ground: bool,
    ) -> Self {
        Self {
            entity_id,
            x,
            y,
            z,
            velocity_x,
            velocity_y,
            velocity_z,
            yaw,
            pitch,
            on_ground,
        }
    }

    /// Returns the `entity_id` field.
    pub fn entity_id(&self) -> i32 {
        self.entity_id
    }

    /// Returns the `x` field.
    pub fn x(&self) -> f64 {
        self.x
    }

    /// Returns the `y` field.
    pub fn y(&self) -> f64 {
        self.y
    }

    /// Returns the `z` field.
    pub fn z(&self) -> f64 {
        self.z
    }

    /// Returns the `velocity_x` field.
    pub fn velocity_x(&self) -> f64 {
        self.velocity_x
    }

    /// Returns the `velocity_y` field.
    pub fn velocity_y(&self) -> f64 {
        self.velocity_y
    }

    /// Returns the `velocity_z` field.
    pub fn velocity_z(&self) -> f64 {
        self.velocity_z
    }

    /// Returns the `yaw` field.
    pub fn yaw(&self) -> f32 {
        self.yaw
    }

    /// Returns the `pitch` field.
    pub fn pitch(&self) -> f32 {
        self.pitch
    }

    /// Returns the `on_ground` field.
    pub fn on_ground(&self) -> bool {
        self.on_ground
    }

    /// Decodes a `EntityTeleport` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let entity_id = reader.read_var_int()?;
        let x = reader.read_f64()?;
        let y = reader.read_f64()?;
        let z = reader.read_f64()?;
        let velocity_x = reader.read_f64()?;
        let velocity_y = reader.read_f64()?;
        let velocity_z = reader.read_f64()?;
        let yaw = reader.read_f32()?;
        let pitch = reader.read_f32()?;
        let on_ground = wire::read_bool(reader)?;
        Ok(Self {
            entity_id,
            x,
            y,
            z,
            velocity_x,
            velocity_y,
            velocity_z,
            yaw,
            pitch,
            on_ground,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        ferrumc_codec::write_var_int(buf, self.entity_id);
        wire::write_f64(buf, self.x);
        wire::write_f64(buf, self.y);
        wire::write_f64(buf, self.z);
        wire::write_f64(buf, self.velocity_x);
        wire::write_f64(buf, self.velocity_y);
        wire::write_f64(buf, self.velocity_z);
        wire::write_f32(buf, self.yaw);
        wire::write_f32(buf, self.pitch);
        wire::write_bool(buf, self.on_ground);
        Ok(())
    }
}

/// `UnloadChunk`: the play clientbound packet (wire id `0x21`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct UnloadChunk {
    chunk_z: i32,
    chunk_x: i32,
}

impl UnloadChunk {
    /// The wire packet id for `UnloadChunk`.
    pub const PACKET_ID: i32 = 0x21;

    /// Creates a new `UnloadChunk` from its wire fields.
    pub fn new(chunk_z: i32, chunk_x: i32) -> Self {
        Self { chunk_z, chunk_x }
    }

    /// Returns the `chunk_z` field.
    pub fn chunk_z(&self) -> i32 {
        self.chunk_z
    }

    /// Returns the `chunk_x` field.
    pub fn chunk_x(&self) -> i32 {
        self.chunk_x
    }

    /// Decodes a `UnloadChunk` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let chunk_z = reader.read_i32()?;
        let chunk_x = reader.read_i32()?;
        Ok(Self { chunk_z, chunk_x })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        wire::write_i32(buf, self.chunk_z);
        wire::write_i32(buf, self.chunk_x);
        Ok(())
    }
}

/// `GameEvent`: the play clientbound packet (wire id `0x22`).
#[derive(Debug, Clone, PartialEq)]
pub struct GameEvent {
    reason: u8,
    value: f32,
}

impl GameEvent {
    /// The wire packet id for `GameEvent`.
    pub const PACKET_ID: i32 = 0x22;

    /// Creates a new `GameEvent` from its wire fields.
    pub fn new(reason: u8, value: f32) -> Self {
        Self { reason, value }
    }

    /// Returns the `reason` field.
    pub fn reason(&self) -> u8 {
        self.reason
    }

    /// Returns the `value` field.
    pub fn value(&self) -> f32 {
        self.value
    }

    /// Decodes a `GameEvent` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let reason = reader.read_u8()?;
        let value = reader.read_f32()?;
        Ok(Self { reason, value })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        wire::write_u8(buf, self.reason);
        wire::write_f32(buf, self.value);
        Ok(())
    }
}

/// `ClientboundKeepAlive`: the play clientbound packet (wire id `0x26`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct ClientboundKeepAlive {
    keep_alive_id: i64,
}

impl ClientboundKeepAlive {
    /// The wire packet id for `ClientboundKeepAlive`.
    pub const PACKET_ID: i32 = 0x26;

    /// Creates a new `ClientboundKeepAlive` from its wire fields.
    pub fn new(keep_alive_id: i64) -> Self {
        Self { keep_alive_id }
    }

    /// Returns the `keep_alive_id` field.
    pub fn keep_alive_id(&self) -> i64 {
        self.keep_alive_id
    }

    /// Decodes a `ClientboundKeepAlive` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let keep_alive_id = reader.read_i64()?;
        Ok(Self { keep_alive_id })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        wire::write_i64(buf, self.keep_alive_id);
        Ok(())
    }
}

/// `ChunkDataAndLight`: the play clientbound packet (wire id `0x27`).
#[derive(Debug, Clone, PartialEq)]
pub struct ChunkDataAndLight {
    x: i32,
    z: i32,
    heightmaps: Vec<Heightmap>,
    chunk_data: ferrumc_codec::BoundedBytes<2_097_152>,
    block_entities: Vec<ChunkBlockEntity>,
    sky_light_mask: Vec<i64>,
    block_light_mask: Vec<i64>,
    empty_sky_light_mask: Vec<i64>,
    empty_block_light_mask: Vec<i64>,
    sky_light: Vec<ferrumc_codec::BoundedBytes<2048>>,
    block_light: Vec<ferrumc_codec::BoundedBytes<2048>>,
}

impl ChunkDataAndLight {
    /// The wire packet id for `ChunkDataAndLight`.
    pub const PACKET_ID: i32 = 0x27;

    /// Creates a new `ChunkDataAndLight` from its wire fields.
    #[allow(
        clippy::too_many_arguments,
        reason = "mirrors the packet's wire fields"
    )]
    pub fn new(
        x: i32,
        z: i32,
        heightmaps: Vec<Heightmap>,
        chunk_data: ferrumc_codec::BoundedBytes<2_097_152>,
        block_entities: Vec<ChunkBlockEntity>,
        sky_light_mask: Vec<i64>,
        block_light_mask: Vec<i64>,
        empty_sky_light_mask: Vec<i64>,
        empty_block_light_mask: Vec<i64>,
        sky_light: Vec<ferrumc_codec::BoundedBytes<2048>>,
        block_light: Vec<ferrumc_codec::BoundedBytes<2048>>,
    ) -> Self {
        Self {
            x,
            z,
            heightmaps,
            chunk_data,
            block_entities,
            sky_light_mask,
            block_light_mask,
            empty_sky_light_mask,
            empty_block_light_mask,
            sky_light,
            block_light,
        }
    }

    /// Returns the `x` field.
    pub fn x(&self) -> i32 {
        self.x
    }

    /// Returns the `z` field.
    pub fn z(&self) -> i32 {
        self.z
    }

    /// Returns the `heightmaps` field.
    pub fn heightmaps(&self) -> &[Heightmap] {
        &self.heightmaps
    }

    /// Returns the `chunk_data` field.
    pub fn chunk_data(&self) -> &ferrumc_codec::BoundedBytes<2_097_152> {
        &self.chunk_data
    }

    /// Returns the `block_entities` field.
    pub fn block_entities(&self) -> &[ChunkBlockEntity] {
        &self.block_entities
    }

    /// Returns the `sky_light_mask` field.
    pub fn sky_light_mask(&self) -> &[i64] {
        &self.sky_light_mask
    }

    /// Returns the `block_light_mask` field.
    pub fn block_light_mask(&self) -> &[i64] {
        &self.block_light_mask
    }

    /// Returns the `empty_sky_light_mask` field.
    pub fn empty_sky_light_mask(&self) -> &[i64] {
        &self.empty_sky_light_mask
    }

    /// Returns the `empty_block_light_mask` field.
    pub fn empty_block_light_mask(&self) -> &[i64] {
        &self.empty_block_light_mask
    }

    /// Returns the `sky_light` field.
    pub fn sky_light(&self) -> &[ferrumc_codec::BoundedBytes<2048>] {
        &self.sky_light
    }

    /// Returns the `block_light` field.
    pub fn block_light(&self) -> &[ferrumc_codec::BoundedBytes<2048>] {
        &self.block_light
    }

    /// Decodes a `ChunkDataAndLight` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let x = reader.read_i32()?;
        let z = reader.read_i32()?;
        let heightmaps = {
            let count = wire::read_prefixed_len(reader)?;
            let mut items = Vec::with_capacity(count.min(reader.remaining()));
            for _ in 0..count {
                items.push(Heightmap::decode(reader)?);
            }
            items
        };
        let chunk_data = ferrumc_codec::BoundedBytes::<2_097_152>::read(reader)?;
        let block_entities = {
            let count = wire::read_prefixed_len(reader)?;
            let mut items = Vec::with_capacity(count.min(reader.remaining()));
            for _ in 0..count {
                items.push(ChunkBlockEntity::decode(reader)?);
            }
            items
        };
        let sky_light_mask = {
            let count = wire::read_prefixed_len(reader)?;
            let mut items = Vec::with_capacity(count.min(reader.remaining()));
            for _ in 0..count {
                items.push(reader.read_i64()?);
            }
            items
        };
        let block_light_mask = {
            let count = wire::read_prefixed_len(reader)?;
            let mut items = Vec::with_capacity(count.min(reader.remaining()));
            for _ in 0..count {
                items.push(reader.read_i64()?);
            }
            items
        };
        let empty_sky_light_mask = {
            let count = wire::read_prefixed_len(reader)?;
            let mut items = Vec::with_capacity(count.min(reader.remaining()));
            for _ in 0..count {
                items.push(reader.read_i64()?);
            }
            items
        };
        let empty_block_light_mask = {
            let count = wire::read_prefixed_len(reader)?;
            let mut items = Vec::with_capacity(count.min(reader.remaining()));
            for _ in 0..count {
                items.push(reader.read_i64()?);
            }
            items
        };
        let sky_light = {
            let count = wire::read_prefixed_len(reader)?;
            let mut items = Vec::with_capacity(count.min(reader.remaining()));
            for _ in 0..count {
                items.push(ferrumc_codec::BoundedBytes::<2048>::read(reader)?);
            }
            items
        };
        let block_light = {
            let count = wire::read_prefixed_len(reader)?;
            let mut items = Vec::with_capacity(count.min(reader.remaining()));
            for _ in 0..count {
                items.push(ferrumc_codec::BoundedBytes::<2048>::read(reader)?);
            }
            items
        };
        Ok(Self {
            x,
            z,
            heightmaps,
            chunk_data,
            block_entities,
            sky_light_mask,
            block_light_mask,
            empty_sky_light_mask,
            empty_block_light_mask,
            sky_light,
            block_light,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        wire::write_i32(buf, self.x);
        wire::write_i32(buf, self.z);
        wire::write_prefixed_len(buf, self.heightmaps.len());
        for elem in &self.heightmaps {
            elem.encode(buf)?;
        }
        self.chunk_data.write(buf);
        wire::write_prefixed_len(buf, self.block_entities.len());
        for elem in &self.block_entities {
            elem.encode(buf)?;
        }
        wire::write_prefixed_len(buf, self.sky_light_mask.len());
        for elem in &self.sky_light_mask {
            wire::write_i64(buf, *elem);
        }
        wire::write_prefixed_len(buf, self.block_light_mask.len());
        for elem in &self.block_light_mask {
            wire::write_i64(buf, *elem);
        }
        wire::write_prefixed_len(buf, self.empty_sky_light_mask.len());
        for elem in &self.empty_sky_light_mask {
            wire::write_i64(buf, *elem);
        }
        wire::write_prefixed_len(buf, self.empty_block_light_mask.len());
        for elem in &self.empty_block_light_mask {
            wire::write_i64(buf, *elem);
        }
        wire::write_prefixed_len(buf, self.sky_light.len());
        for elem in &self.sky_light {
            elem.write(buf);
        }
        wire::write_prefixed_len(buf, self.block_light.len());
        for elem in &self.block_light {
            elem.write(buf);
        }
        Ok(())
    }
}

/// `Particle`: the play clientbound packet (wire id `0x29`).
#[derive(Debug, Clone, PartialEq)]
pub struct Particle {
    long_distance: bool,
    always_show: bool,
    x: f64,
    y: f64,
    z: f64,
    offset_x: f32,
    offset_y: f32,
    offset_z: f32,
    max_speed: f32,
    count: i32,
    kind: i32,
    data: Vec<u8>,
}

impl Particle {
    /// The wire packet id for `Particle`.
    pub const PACKET_ID: i32 = 0x29;

    /// Creates a new `Particle` from its wire fields.
    #[allow(
        clippy::too_many_arguments,
        reason = "mirrors the packet's wire fields"
    )]
    pub fn new(
        long_distance: bool,
        always_show: bool,
        x: f64,
        y: f64,
        z: f64,
        offset_x: f32,
        offset_y: f32,
        offset_z: f32,
        max_speed: f32,
        count: i32,
        kind: i32,
        data: Vec<u8>,
    ) -> Self {
        Self {
            long_distance,
            always_show,
            x,
            y,
            z,
            offset_x,
            offset_y,
            offset_z,
            max_speed,
            count,
            kind,
            data,
        }
    }

    /// Returns the `long_distance` field.
    pub fn long_distance(&self) -> bool {
        self.long_distance
    }

    /// Returns the `always_show` field.
    pub fn always_show(&self) -> bool {
        self.always_show
    }

    /// Returns the `x` field.
    pub fn x(&self) -> f64 {
        self.x
    }

    /// Returns the `y` field.
    pub fn y(&self) -> f64 {
        self.y
    }

    /// Returns the `z` field.
    pub fn z(&self) -> f64 {
        self.z
    }

    /// Returns the `offset_x` field.
    pub fn offset_x(&self) -> f32 {
        self.offset_x
    }

    /// Returns the `offset_y` field.
    pub fn offset_y(&self) -> f32 {
        self.offset_y
    }

    /// Returns the `offset_z` field.
    pub fn offset_z(&self) -> f32 {
        self.offset_z
    }

    /// Returns the `max_speed` field.
    pub fn max_speed(&self) -> f32 {
        self.max_speed
    }

    /// Returns the `count` field.
    pub fn count(&self) -> i32 {
        self.count
    }

    /// Returns the `kind` field.
    pub fn kind(&self) -> i32 {
        self.kind
    }

    /// Returns the `data` field.
    pub fn data(&self) -> &[u8] {
        &self.data
    }

    /// Decodes a `Particle` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let long_distance = wire::read_bool(reader)?;
        let always_show = wire::read_bool(reader)?;
        let x = reader.read_f64()?;
        let y = reader.read_f64()?;
        let z = reader.read_f64()?;
        let offset_x = reader.read_f32()?;
        let offset_y = reader.read_f32()?;
        let offset_z = reader.read_f32()?;
        let max_speed = reader.read_f32()?;
        let count = reader.read_i32()?;
        let kind = reader.read_var_int()?;
        let data = {
            let remaining = reader.remaining();
            reader.read_bytes(remaining)?.to_vec()
        };
        Ok(Self {
            long_distance,
            always_show,
            x,
            y,
            z,
            offset_x,
            offset_y,
            offset_z,
            max_speed,
            count,
            kind,
            data,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        wire::write_bool(buf, self.long_distance);
        wire::write_bool(buf, self.always_show);
        wire::write_f64(buf, self.x);
        wire::write_f64(buf, self.y);
        wire::write_f64(buf, self.z);
        wire::write_f32(buf, self.offset_x);
        wire::write_f32(buf, self.offset_y);
        wire::write_f32(buf, self.offset_z);
        wire::write_f32(buf, self.max_speed);
        wire::write_i32(buf, self.count);
        ferrumc_codec::write_var_int(buf, self.kind);
        wire::write_raw(buf, &self.data);
        Ok(())
    }
}

/// `JoinGame`: the play clientbound packet (wire id `0x2b`).
#[allow(
    clippy::struct_excessive_bools,
    reason = "mirrors the packet's wire fields"
)]
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct JoinGame {
    entity_id: i32,
    is_hardcore: bool,
    world_names: Vec<BoundedString<32_767>>,
    max_players: i32,
    view_distance: i32,
    simulation_distance: i32,
    reduced_debug_info: bool,
    enable_respawn_screen: bool,
    do_limited_crafting: bool,
    world_state: SpawnInfo,
    enforces_secure_chat: bool,
}

impl JoinGame {
    /// The wire packet id for `JoinGame`.
    pub const PACKET_ID: i32 = 0x2b;

    /// Creates a new `JoinGame` from its wire fields.
    #[allow(
        clippy::too_many_arguments,
        clippy::fn_params_excessive_bools,
        reason = "mirrors the packet's wire fields"
    )]
    pub fn new(
        entity_id: i32,
        is_hardcore: bool,
        world_names: Vec<BoundedString<32_767>>,
        max_players: i32,
        view_distance: i32,
        simulation_distance: i32,
        reduced_debug_info: bool,
        enable_respawn_screen: bool,
        do_limited_crafting: bool,
        world_state: SpawnInfo,
        enforces_secure_chat: bool,
    ) -> Self {
        Self {
            entity_id,
            is_hardcore,
            world_names,
            max_players,
            view_distance,
            simulation_distance,
            reduced_debug_info,
            enable_respawn_screen,
            do_limited_crafting,
            world_state,
            enforces_secure_chat,
        }
    }

    /// Returns the `entity_id` field.
    pub fn entity_id(&self) -> i32 {
        self.entity_id
    }

    /// Returns the `is_hardcore` field.
    pub fn is_hardcore(&self) -> bool {
        self.is_hardcore
    }

    /// Returns the `world_names` field.
    pub fn world_names(&self) -> &[BoundedString<32_767>] {
        &self.world_names
    }

    /// Returns the `max_players` field.
    pub fn max_players(&self) -> i32 {
        self.max_players
    }

    /// Returns the `view_distance` field.
    pub fn view_distance(&self) -> i32 {
        self.view_distance
    }

    /// Returns the `simulation_distance` field.
    pub fn simulation_distance(&self) -> i32 {
        self.simulation_distance
    }

    /// Returns the `reduced_debug_info` field.
    pub fn reduced_debug_info(&self) -> bool {
        self.reduced_debug_info
    }

    /// Returns the `enable_respawn_screen` field.
    pub fn enable_respawn_screen(&self) -> bool {
        self.enable_respawn_screen
    }

    /// Returns the `do_limited_crafting` field.
    pub fn do_limited_crafting(&self) -> bool {
        self.do_limited_crafting
    }

    /// Returns the `world_state` field.
    pub fn world_state(&self) -> &SpawnInfo {
        &self.world_state
    }

    /// Returns the `enforces_secure_chat` field.
    pub fn enforces_secure_chat(&self) -> bool {
        self.enforces_secure_chat
    }

    /// Decodes a `JoinGame` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let entity_id = reader.read_i32()?;
        let is_hardcore = wire::read_bool(reader)?;
        let world_names = {
            let count = wire::read_prefixed_len(reader)?;
            let mut items = Vec::with_capacity(count.min(reader.remaining()));
            for _ in 0..count {
                items.push(BoundedString::<32_767>::read(reader)?);
            }
            items
        };
        let max_players = reader.read_var_int()?;
        let view_distance = reader.read_var_int()?;
        let simulation_distance = reader.read_var_int()?;
        let reduced_debug_info = wire::read_bool(reader)?;
        let enable_respawn_screen = wire::read_bool(reader)?;
        let do_limited_crafting = wire::read_bool(reader)?;
        let world_state = SpawnInfo::decode(reader)?;
        let enforces_secure_chat = wire::read_bool(reader)?;
        Ok(Self {
            entity_id,
            is_hardcore,
            world_names,
            max_players,
            view_distance,
            simulation_distance,
            reduced_debug_info,
            enable_respawn_screen,
            do_limited_crafting,
            world_state,
            enforces_secure_chat,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        wire::write_i32(buf, self.entity_id);
        wire::write_bool(buf, self.is_hardcore);
        wire::write_prefixed_len(buf, self.world_names.len());
        for elem in &self.world_names {
            elem.write(buf);
        }
        ferrumc_codec::write_var_int(buf, self.max_players);
        ferrumc_codec::write_var_int(buf, self.view_distance);
        ferrumc_codec::write_var_int(buf, self.simulation_distance);
        wire::write_bool(buf, self.reduced_debug_info);
        wire::write_bool(buf, self.enable_respawn_screen);
        wire::write_bool(buf, self.do_limited_crafting);
        self.world_state.encode(buf)?;
        wire::write_bool(buf, self.enforces_secure_chat);
        Ok(())
    }
}

/// `UpdateEntityPosition`: the play clientbound packet (wire id `0x2e`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct UpdateEntityPosition {
    entity_id: i32,
    delta_x: i16,
    delta_y: i16,
    delta_z: i16,
    on_ground: bool,
}

impl UpdateEntityPosition {
    /// The wire packet id for `UpdateEntityPosition`.
    pub const PACKET_ID: i32 = 0x2e;

    /// Creates a new `UpdateEntityPosition` from its wire fields.
    pub fn new(entity_id: i32, delta_x: i16, delta_y: i16, delta_z: i16, on_ground: bool) -> Self {
        Self {
            entity_id,
            delta_x,
            delta_y,
            delta_z,
            on_ground,
        }
    }

    /// Returns the `entity_id` field.
    pub fn entity_id(&self) -> i32 {
        self.entity_id
    }

    /// Returns the `delta_x` field.
    pub fn delta_x(&self) -> i16 {
        self.delta_x
    }

    /// Returns the `delta_y` field.
    pub fn delta_y(&self) -> i16 {
        self.delta_y
    }

    /// Returns the `delta_z` field.
    pub fn delta_z(&self) -> i16 {
        self.delta_z
    }

    /// Returns the `on_ground` field.
    pub fn on_ground(&self) -> bool {
        self.on_ground
    }

    /// Decodes a `UpdateEntityPosition` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let entity_id = reader.read_var_int()?;
        let delta_x = reader.read_i16()?;
        let delta_y = reader.read_i16()?;
        let delta_z = reader.read_i16()?;
        let on_ground = wire::read_bool(reader)?;
        Ok(Self {
            entity_id,
            delta_x,
            delta_y,
            delta_z,
            on_ground,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        ferrumc_codec::write_var_int(buf, self.entity_id);
        wire::write_i16(buf, self.delta_x);
        wire::write_i16(buf, self.delta_y);
        wire::write_i16(buf, self.delta_z);
        wire::write_bool(buf, self.on_ground);
        Ok(())
    }
}

/// `UpdateEntityPositionAndRotation`: the play clientbound packet (wire id `0x2f`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct UpdateEntityPositionAndRotation {
    entity_id: i32,
    delta_x: i16,
    delta_y: i16,
    delta_z: i16,
    yaw: i8,
    pitch: i8,
    on_ground: bool,
}

impl UpdateEntityPositionAndRotation {
    /// The wire packet id for `UpdateEntityPositionAndRotation`.
    pub const PACKET_ID: i32 = 0x2f;

    /// Creates a new `UpdateEntityPositionAndRotation` from its wire fields.
    pub fn new(
        entity_id: i32,
        delta_x: i16,
        delta_y: i16,
        delta_z: i16,
        yaw: i8,
        pitch: i8,
        on_ground: bool,
    ) -> Self {
        Self {
            entity_id,
            delta_x,
            delta_y,
            delta_z,
            yaw,
            pitch,
            on_ground,
        }
    }

    /// Returns the `entity_id` field.
    pub fn entity_id(&self) -> i32 {
        self.entity_id
    }

    /// Returns the `delta_x` field.
    pub fn delta_x(&self) -> i16 {
        self.delta_x
    }

    /// Returns the `delta_y` field.
    pub fn delta_y(&self) -> i16 {
        self.delta_y
    }

    /// Returns the `delta_z` field.
    pub fn delta_z(&self) -> i16 {
        self.delta_z
    }

    /// Returns the `yaw` field.
    pub fn yaw(&self) -> i8 {
        self.yaw
    }

    /// Returns the `pitch` field.
    pub fn pitch(&self) -> i8 {
        self.pitch
    }

    /// Returns the `on_ground` field.
    pub fn on_ground(&self) -> bool {
        self.on_ground
    }

    /// Decodes a `UpdateEntityPositionAndRotation` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let entity_id = reader.read_var_int()?;
        let delta_x = reader.read_i16()?;
        let delta_y = reader.read_i16()?;
        let delta_z = reader.read_i16()?;
        let yaw = reader.read_i8()?;
        let pitch = reader.read_i8()?;
        let on_ground = wire::read_bool(reader)?;
        Ok(Self {
            entity_id,
            delta_x,
            delta_y,
            delta_z,
            yaw,
            pitch,
            on_ground,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        ferrumc_codec::write_var_int(buf, self.entity_id);
        wire::write_i16(buf, self.delta_x);
        wire::write_i16(buf, self.delta_y);
        wire::write_i16(buf, self.delta_z);
        wire::write_i8(buf, self.yaw);
        wire::write_i8(buf, self.pitch);
        wire::write_bool(buf, self.on_ground);
        Ok(())
    }
}

/// `UpdateEntityRotation`: the play clientbound packet (wire id `0x31`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct UpdateEntityRotation {
    entity_id: i32,
    yaw: i8,
    pitch: i8,
    on_ground: bool,
}

impl UpdateEntityRotation {
    /// The wire packet id for `UpdateEntityRotation`.
    pub const PACKET_ID: i32 = 0x31;

    /// Creates a new `UpdateEntityRotation` from its wire fields.
    pub fn new(entity_id: i32, yaw: i8, pitch: i8, on_ground: bool) -> Self {
        Self {
            entity_id,
            yaw,
            pitch,
            on_ground,
        }
    }

    /// Returns the `entity_id` field.
    pub fn entity_id(&self) -> i32 {
        self.entity_id
    }

    /// Returns the `yaw` field.
    pub fn yaw(&self) -> i8 {
        self.yaw
    }

    /// Returns the `pitch` field.
    pub fn pitch(&self) -> i8 {
        self.pitch
    }

    /// Returns the `on_ground` field.
    pub fn on_ground(&self) -> bool {
        self.on_ground
    }

    /// Decodes a `UpdateEntityRotation` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let entity_id = reader.read_var_int()?;
        let yaw = reader.read_i8()?;
        let pitch = reader.read_i8()?;
        let on_ground = wire::read_bool(reader)?;
        Ok(Self {
            entity_id,
            yaw,
            pitch,
            on_ground,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        ferrumc_codec::write_var_int(buf, self.entity_id);
        wire::write_i8(buf, self.yaw);
        wire::write_i8(buf, self.pitch);
        wire::write_bool(buf, self.on_ground);
        Ok(())
    }
}

/// `OpenSignEditor`: the play clientbound packet (wire id `0x35`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct OpenSignEditor {
    location: crate::types::BlockPosition,
    is_front_text: bool,
}

impl OpenSignEditor {
    /// The wire packet id for `OpenSignEditor`.
    pub const PACKET_ID: i32 = 0x35;

    /// Creates a new `OpenSignEditor` from its wire fields.
    pub fn new(location: crate::types::BlockPosition, is_front_text: bool) -> Self {
        Self {
            location,
            is_front_text,
        }
    }

    /// Returns the `location` field.
    pub fn location(&self) -> crate::types::BlockPosition {
        self.location
    }

    /// Returns the `is_front_text` field.
    pub fn is_front_text(&self) -> bool {
        self.is_front_text
    }

    /// Decodes a `OpenSignEditor` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let location = crate::types::BlockPosition::read(reader)?;
        let is_front_text = wire::read_bool(reader)?;
        Ok(Self {
            location,
            is_front_text,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        self.location.write(buf);
        wire::write_bool(buf, self.is_front_text);
        Ok(())
    }
}

/// `PlayerAbilities`: the play clientbound packet (wire id `0x39`).
#[derive(Debug, Clone, PartialEq)]
pub struct PlayerAbilities {
    flags: i8,
    flying_speed: f32,
    walking_speed: f32,
}

impl PlayerAbilities {
    /// The wire packet id for `PlayerAbilities`.
    pub const PACKET_ID: i32 = 0x39;

    /// Creates a new `PlayerAbilities` from its wire fields.
    pub fn new(flags: i8, flying_speed: f32, walking_speed: f32) -> Self {
        Self {
            flags,
            flying_speed,
            walking_speed,
        }
    }

    /// Returns the `flags` field.
    pub fn flags(&self) -> i8 {
        self.flags
    }

    /// Returns the `flying_speed` field.
    pub fn flying_speed(&self) -> f32 {
        self.flying_speed
    }

    /// Returns the `walking_speed` field.
    pub fn walking_speed(&self) -> f32 {
        self.walking_speed
    }

    /// Decodes a `PlayerAbilities` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let flags = reader.read_i8()?;
        let flying_speed = reader.read_f32()?;
        let walking_speed = reader.read_f32()?;
        Ok(Self {
            flags,
            flying_speed,
            walking_speed,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        wire::write_i8(buf, self.flags);
        wire::write_f32(buf, self.flying_speed);
        wire::write_f32(buf, self.walking_speed);
        Ok(())
    }
}

/// `RemovePlayerInfo`: the play clientbound packet (wire id `0x3e`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct RemovePlayerInfo {
    players: Vec<uuid::Uuid>,
}

impl RemovePlayerInfo {
    /// The wire packet id for `RemovePlayerInfo`.
    pub const PACKET_ID: i32 = 0x3e;

    /// Creates a new `RemovePlayerInfo` from its wire fields.
    pub fn new(players: Vec<uuid::Uuid>) -> Self {
        Self { players }
    }

    /// Returns the `players` field.
    pub fn players(&self) -> &[uuid::Uuid] {
        &self.players
    }

    /// Decodes a `RemovePlayerInfo` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let players = {
            let count = wire::read_prefixed_len(reader)?;
            let mut items = Vec::with_capacity(count.min(reader.remaining()));
            for _ in 0..count {
                items.push(wire::read_uuid(reader)?);
            }
            items
        };
        Ok(Self { players })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        wire::write_prefixed_len(buf, self.players.len());
        for elem in &self.players {
            wire::write_uuid(buf, *elem);
        }
        Ok(())
    }
}

/// `PlayerInfoUpdate`: the play clientbound packet (wire id `0x3f`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct PlayerInfoUpdate {
    action: u8,
    entries: Vec<u8>,
}

impl PlayerInfoUpdate {
    /// The wire packet id for `PlayerInfoUpdate`.
    pub const PACKET_ID: i32 = 0x3f;

    /// Creates a new `PlayerInfoUpdate` from its wire fields.
    pub fn new(action: u8, entries: Vec<u8>) -> Self {
        Self { action, entries }
    }

    /// Returns the `action` field.
    pub fn action(&self) -> u8 {
        self.action
    }

    /// Returns the `entries` field.
    pub fn entries(&self) -> &[u8] {
        &self.entries
    }

    /// Decodes a `PlayerInfoUpdate` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let action = reader.read_u8()?;
        let entries = {
            let remaining = reader.remaining();
            reader.read_bytes(remaining)?.to_vec()
        };
        Ok(Self { action, entries })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        wire::write_u8(buf, self.action);
        wire::write_raw(buf, &self.entries);
        Ok(())
    }
}

/// `SynchronizePlayerPosition`: the play clientbound packet (wire id `0x41`).
#[derive(Debug, Clone, PartialEq)]
pub struct SynchronizePlayerPosition {
    teleport_id: i32,
    x: f64,
    y: f64,
    z: f64,
    dx: f64,
    dy: f64,
    dz: f64,
    yaw: f32,
    pitch: f32,
    flags: u32,
}

impl SynchronizePlayerPosition {
    /// The wire packet id for `SynchronizePlayerPosition`.
    pub const PACKET_ID: i32 = 0x41;

    /// Creates a new `SynchronizePlayerPosition` from its wire fields.
    #[allow(
        clippy::too_many_arguments,
        reason = "mirrors the packet's wire fields"
    )]
    pub fn new(
        teleport_id: i32,
        x: f64,
        y: f64,
        z: f64,
        dx: f64,
        dy: f64,
        dz: f64,
        yaw: f32,
        pitch: f32,
        flags: u32,
    ) -> Self {
        Self {
            teleport_id,
            x,
            y,
            z,
            dx,
            dy,
            dz,
            yaw,
            pitch,
            flags,
        }
    }

    /// Returns the `teleport_id` field.
    pub fn teleport_id(&self) -> i32 {
        self.teleport_id
    }

    /// Returns the `x` field.
    pub fn x(&self) -> f64 {
        self.x
    }

    /// Returns the `y` field.
    pub fn y(&self) -> f64 {
        self.y
    }

    /// Returns the `z` field.
    pub fn z(&self) -> f64 {
        self.z
    }

    /// Returns the `dx` field.
    pub fn dx(&self) -> f64 {
        self.dx
    }

    /// Returns the `dy` field.
    pub fn dy(&self) -> f64 {
        self.dy
    }

    /// Returns the `dz` field.
    pub fn dz(&self) -> f64 {
        self.dz
    }

    /// Returns the `yaw` field.
    pub fn yaw(&self) -> f32 {
        self.yaw
    }

    /// Returns the `pitch` field.
    pub fn pitch(&self) -> f32 {
        self.pitch
    }

    /// Returns the `flags` field.
    pub fn flags(&self) -> u32 {
        self.flags
    }

    /// Decodes a `SynchronizePlayerPosition` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let teleport_id = reader.read_var_int()?;
        let x = reader.read_f64()?;
        let y = reader.read_f64()?;
        let z = reader.read_f64()?;
        let dx = reader.read_f64()?;
        let dy = reader.read_f64()?;
        let dz = reader.read_f64()?;
        let yaw = reader.read_f32()?;
        let pitch = reader.read_f32()?;
        let flags = reader.read_u32()?;
        Ok(Self {
            teleport_id,
            x,
            y,
            z,
            dx,
            dy,
            dz,
            yaw,
            pitch,
            flags,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        ferrumc_codec::write_var_int(buf, self.teleport_id);
        wire::write_f64(buf, self.x);
        wire::write_f64(buf, self.y);
        wire::write_f64(buf, self.z);
        wire::write_f64(buf, self.dx);
        wire::write_f64(buf, self.dy);
        wire::write_f64(buf, self.dz);
        wire::write_f32(buf, self.yaw);
        wire::write_f32(buf, self.pitch);
        wire::write_u32(buf, self.flags);
        Ok(())
    }
}

/// `RemoveEntities`: the play clientbound packet (wire id `0x46`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct RemoveEntities {
    entity_ids: Vec<i32>,
}

impl RemoveEntities {
    /// The wire packet id for `RemoveEntities`.
    pub const PACKET_ID: i32 = 0x46;

    /// Creates a new `RemoveEntities` from its wire fields.
    pub fn new(entity_ids: Vec<i32>) -> Self {
        Self { entity_ids }
    }

    /// Returns the `entity_ids` field.
    pub fn entity_ids(&self) -> &[i32] {
        &self.entity_ids
    }

    /// Decodes a `RemoveEntities` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let entity_ids = {
            let count = wire::read_prefixed_len(reader)?;
            let mut items = Vec::with_capacity(count.min(reader.remaining()));
            for _ in 0..count {
                items.push(reader.read_var_int()?);
            }
            items
        };
        Ok(Self { entity_ids })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        wire::write_prefixed_len(buf, self.entity_ids.len());
        for elem in &self.entity_ids {
            ferrumc_codec::write_var_int(buf, *elem);
        }
        Ok(())
    }
}

/// `SetHeadRotation`: the play clientbound packet (wire id `0x4c`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct SetHeadRotation {
    entity_id: i32,
    head_yaw: i8,
}

impl SetHeadRotation {
    /// The wire packet id for `SetHeadRotation`.
    pub const PACKET_ID: i32 = 0x4c;

    /// Creates a new `SetHeadRotation` from its wire fields.
    pub fn new(entity_id: i32, head_yaw: i8) -> Self {
        Self {
            entity_id,
            head_yaw,
        }
    }

    /// Returns the `entity_id` field.
    pub fn entity_id(&self) -> i32 {
        self.entity_id
    }

    /// Returns the `head_yaw` field.
    pub fn head_yaw(&self) -> i8 {
        self.head_yaw
    }

    /// Decodes a `SetHeadRotation` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let entity_id = reader.read_var_int()?;
        let head_yaw = reader.read_i8()?;
        Ok(Self {
            entity_id,
            head_yaw,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        ferrumc_codec::write_var_int(buf, self.entity_id);
        wire::write_i8(buf, self.head_yaw);
        Ok(())
    }
}

/// `SetActionBarText`: the play clientbound packet (wire id `0x50`).
#[derive(Debug, Clone, PartialEq)]
pub struct SetActionBarText {
    text: ferrumc_nbt::NbtTag,
}

impl SetActionBarText {
    /// The wire packet id for `SetActionBarText`.
    pub const PACKET_ID: i32 = 0x50;

    /// Creates a new `SetActionBarText` from its wire fields.
    pub fn new(text: ferrumc_nbt::NbtTag) -> Self {
        Self { text }
    }

    /// Returns the `text` field.
    pub fn text(&self) -> &ferrumc_nbt::NbtTag {
        &self.text
    }

    /// Decodes a `SetActionBarText` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let text = {
            let available = reader.remaining();
            let bytes = reader.read_bytes(available)?;
            let (tag, consumed) = ferrumc_nbt::read_network_root_with_consumed(
                bytes,
                &ferrumc_nbt::NbtLimits::default(),
            )?;
            *reader = BoundedReader::new(bytes.get(consumed..).unwrap_or_default());
            tag
        };
        Ok(Self { text })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        {
            let nbt_bytes =
                ferrumc_nbt::write_network_root(&self.text, &ferrumc_nbt::NbtLimits::default())?;
            wire::write_raw(buf, &nbt_bytes);
        }
        Ok(())
    }
}

/// `SetCenterChunk`: the play clientbound packet (wire id `0x57`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct SetCenterChunk {
    chunk_x: i32,
    chunk_z: i32,
}

impl SetCenterChunk {
    /// The wire packet id for `SetCenterChunk`.
    pub const PACKET_ID: i32 = 0x57;

    /// Creates a new `SetCenterChunk` from its wire fields.
    pub fn new(chunk_x: i32, chunk_z: i32) -> Self {
        Self { chunk_x, chunk_z }
    }

    /// Returns the `chunk_x` field.
    pub fn chunk_x(&self) -> i32 {
        self.chunk_x
    }

    /// Returns the `chunk_z` field.
    pub fn chunk_z(&self) -> i32 {
        self.chunk_z
    }

    /// Decodes a `SetCenterChunk` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let chunk_x = reader.read_var_int()?;
        let chunk_z = reader.read_var_int()?;
        Ok(Self { chunk_x, chunk_z })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        ferrumc_codec::write_var_int(buf, self.chunk_x);
        ferrumc_codec::write_var_int(buf, self.chunk_z);
        Ok(())
    }
}

/// `SetDefaultSpawnPosition`: the play clientbound packet (wire id `0x5a`).
#[derive(Debug, Clone, PartialEq)]
pub struct SetDefaultSpawnPosition {
    location: crate::types::BlockPosition,
    angle: f32,
}

impl SetDefaultSpawnPosition {
    /// The wire packet id for `SetDefaultSpawnPosition`.
    pub const PACKET_ID: i32 = 0x5a;

    /// Creates a new `SetDefaultSpawnPosition` from its wire fields.
    pub fn new(location: crate::types::BlockPosition, angle: f32) -> Self {
        Self { location, angle }
    }

    /// Returns the `location` field.
    pub fn location(&self) -> crate::types::BlockPosition {
        self.location
    }

    /// Returns the `angle` field.
    pub fn angle(&self) -> f32 {
        self.angle
    }

    /// Decodes a `SetDefaultSpawnPosition` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let location = crate::types::BlockPosition::read(reader)?;
        let angle = reader.read_f32()?;
        Ok(Self { location, angle })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        self.location.write(buf);
        wire::write_f32(buf, self.angle);
        Ok(())
    }
}

/// `DisplayObjective`: the play clientbound packet (wire id `0x5b`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct DisplayObjective {
    position: i32,
    name: BoundedString<32_767>,
}

impl DisplayObjective {
    /// The wire packet id for `DisplayObjective`.
    pub const PACKET_ID: i32 = 0x5b;

    /// Creates a new `DisplayObjective` from its wire fields.
    pub fn new(position: i32, name: BoundedString<32_767>) -> Self {
        Self { position, name }
    }

    /// Returns the `position` field.
    pub fn position(&self) -> i32 {
        self.position
    }

    /// Returns the `name` field.
    pub fn name(&self) -> &BoundedString<32_767> {
        &self.name
    }

    /// Decodes a `DisplayObjective` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let position = reader.read_var_int()?;
        let name = BoundedString::<32_767>::read(reader)?;
        Ok(Self { position, name })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        ferrumc_codec::write_var_int(buf, self.position);
        self.name.write(buf);
        Ok(())
    }
}

/// `SetEquipment`: the play clientbound packet (wire id `0x5f`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct SetEquipment {
    entity_id: i32,
    equipments: Vec<u8>,
}

impl SetEquipment {
    /// The wire packet id for `SetEquipment`.
    pub const PACKET_ID: i32 = 0x5f;

    /// Creates a new `SetEquipment` from its wire fields.
    pub fn new(entity_id: i32, equipments: Vec<u8>) -> Self {
        Self {
            entity_id,
            equipments,
        }
    }

    /// Returns the `entity_id` field.
    pub fn entity_id(&self) -> i32 {
        self.entity_id
    }

    /// Returns the `equipments` field.
    pub fn equipments(&self) -> &[u8] {
        &self.equipments
    }

    /// Decodes a `SetEquipment` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let entity_id = reader.read_var_int()?;
        let equipments = {
            let remaining = reader.remaining();
            reader.read_bytes(remaining)?.to_vec()
        };
        Ok(Self {
            entity_id,
            equipments,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        ferrumc_codec::write_var_int(buf, self.entity_id);
        wire::write_raw(buf, &self.equipments);
        Ok(())
    }
}

/// `ClientboundSetHeldItem`: the play clientbound packet (wire id `0x62`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct ClientboundSetHeldItem {
    slot: i32,
}

impl ClientboundSetHeldItem {
    /// The wire packet id for `ClientboundSetHeldItem`.
    pub const PACKET_ID: i32 = 0x62;

    /// Creates a new `ClientboundSetHeldItem` from its wire fields.
    pub fn new(slot: i32) -> Self {
        Self { slot }
    }

    /// Returns the `slot` field.
    pub fn slot(&self) -> i32 {
        self.slot
    }

    /// Decodes a `ClientboundSetHeldItem` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let slot = reader.read_var_int()?;
        Ok(Self { slot })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        ferrumc_codec::write_var_int(buf, self.slot);
        Ok(())
    }
}

/// `UpdateObjectives`: the play clientbound packet (wire id `0x63`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct UpdateObjectives {
    name: BoundedString<32_767>,
    mode: i8,
    rest: Vec<u8>,
}

impl UpdateObjectives {
    /// The wire packet id for `UpdateObjectives`.
    pub const PACKET_ID: i32 = 0x63;

    /// Creates a new `UpdateObjectives` from its wire fields.
    pub fn new(name: BoundedString<32_767>, mode: i8, rest: Vec<u8>) -> Self {
        Self { name, mode, rest }
    }

    /// Returns the `name` field.
    pub fn name(&self) -> &BoundedString<32_767> {
        &self.name
    }

    /// Returns the `mode` field.
    pub fn mode(&self) -> i8 {
        self.mode
    }

    /// Returns the `rest` field.
    pub fn rest(&self) -> &[u8] {
        &self.rest
    }

    /// Decodes a `UpdateObjectives` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let name = BoundedString::<32_767>::read(reader)?;
        let mode = reader.read_i8()?;
        let rest = {
            let remaining = reader.remaining();
            reader.read_bytes(remaining)?.to_vec()
        };
        Ok(Self { name, mode, rest })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        self.name.write(buf);
        wire::write_i8(buf, self.mode);
        wire::write_raw(buf, &self.rest);
        Ok(())
    }
}

/// `SetPlayerTeam`: the play clientbound packet (wire id `0x66`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct SetPlayerTeam {
    name: BoundedString<32_767>,
    method: i8,
    rest: Vec<u8>,
}

impl SetPlayerTeam {
    /// The wire packet id for `SetPlayerTeam`.
    pub const PACKET_ID: i32 = 0x66;

    /// Creates a new `SetPlayerTeam` from its wire fields.
    pub fn new(name: BoundedString<32_767>, method: i8, rest: Vec<u8>) -> Self {
        Self { name, method, rest }
    }

    /// Returns the `name` field.
    pub fn name(&self) -> &BoundedString<32_767> {
        &self.name
    }

    /// Returns the `method` field.
    pub fn method(&self) -> i8 {
        self.method
    }

    /// Returns the `rest` field.
    pub fn rest(&self) -> &[u8] {
        &self.rest
    }

    /// Decodes a `SetPlayerTeam` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let name = BoundedString::<32_767>::read(reader)?;
        let method = reader.read_i8()?;
        let rest = {
            let remaining = reader.remaining();
            reader.read_bytes(remaining)?.to_vec()
        };
        Ok(Self { name, method, rest })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        self.name.write(buf);
        wire::write_i8(buf, self.method);
        wire::write_raw(buf, &self.rest);
        Ok(())
    }
}

/// `UpdateScore`: the play clientbound packet (wire id `0x67`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct UpdateScore {
    entity_name: BoundedString<32_767>,
    objective_name: BoundedString<32_767>,
    value: i32,
    rest: Vec<u8>,
}

impl UpdateScore {
    /// The wire packet id for `UpdateScore`.
    pub const PACKET_ID: i32 = 0x67;

    /// Creates a new `UpdateScore` from its wire fields.
    pub fn new(
        entity_name: BoundedString<32_767>,
        objective_name: BoundedString<32_767>,
        value: i32,
        rest: Vec<u8>,
    ) -> Self {
        Self {
            entity_name,
            objective_name,
            value,
            rest,
        }
    }

    /// Returns the `entity_name` field.
    pub fn entity_name(&self) -> &BoundedString<32_767> {
        &self.entity_name
    }

    /// Returns the `objective_name` field.
    pub fn objective_name(&self) -> &BoundedString<32_767> {
        &self.objective_name
    }

    /// Returns the `value` field.
    pub fn value(&self) -> i32 {
        self.value
    }

    /// Returns the `rest` field.
    pub fn rest(&self) -> &[u8] {
        &self.rest
    }

    /// Decodes a `UpdateScore` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let entity_name = BoundedString::<32_767>::read(reader)?;
        let objective_name = BoundedString::<32_767>::read(reader)?;
        let value = reader.read_var_int()?;
        let rest = {
            let remaining = reader.remaining();
            reader.read_bytes(remaining)?.to_vec()
        };
        Ok(Self {
            entity_name,
            objective_name,
            value,
            rest,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        self.entity_name.write(buf);
        self.objective_name.write(buf);
        ferrumc_codec::write_var_int(buf, self.value);
        wire::write_raw(buf, &self.rest);
        Ok(())
    }
}

/// `SetSubtitleText`: the play clientbound packet (wire id `0x69`).
#[derive(Debug, Clone, PartialEq)]
pub struct SetSubtitleText {
    text: ferrumc_nbt::NbtTag,
}

impl SetSubtitleText {
    /// The wire packet id for `SetSubtitleText`.
    pub const PACKET_ID: i32 = 0x69;

    /// Creates a new `SetSubtitleText` from its wire fields.
    pub fn new(text: ferrumc_nbt::NbtTag) -> Self {
        Self { text }
    }

    /// Returns the `text` field.
    pub fn text(&self) -> &ferrumc_nbt::NbtTag {
        &self.text
    }

    /// Decodes a `SetSubtitleText` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let text = {
            let available = reader.remaining();
            let bytes = reader.read_bytes(available)?;
            let (tag, consumed) = ferrumc_nbt::read_network_root_with_consumed(
                bytes,
                &ferrumc_nbt::NbtLimits::default(),
            )?;
            *reader = BoundedReader::new(bytes.get(consumed..).unwrap_or_default());
            tag
        };
        Ok(Self { text })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        {
            let nbt_bytes =
                ferrumc_nbt::write_network_root(&self.text, &ferrumc_nbt::NbtLimits::default())?;
            wire::write_raw(buf, &nbt_bytes);
        }
        Ok(())
    }
}

/// `SetTitleText`: the play clientbound packet (wire id `0x6b`).
#[derive(Debug, Clone, PartialEq)]
pub struct SetTitleText {
    text: ferrumc_nbt::NbtTag,
}

impl SetTitleText {
    /// The wire packet id for `SetTitleText`.
    pub const PACKET_ID: i32 = 0x6b;

    /// Creates a new `SetTitleText` from its wire fields.
    pub fn new(text: ferrumc_nbt::NbtTag) -> Self {
        Self { text }
    }

    /// Returns the `text` field.
    pub fn text(&self) -> &ferrumc_nbt::NbtTag {
        &self.text
    }

    /// Decodes a `SetTitleText` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let text = {
            let available = reader.remaining();
            let bytes = reader.read_bytes(available)?;
            let (tag, consumed) = ferrumc_nbt::read_network_root_with_consumed(
                bytes,
                &ferrumc_nbt::NbtLimits::default(),
            )?;
            *reader = BoundedReader::new(bytes.get(consumed..).unwrap_or_default());
            tag
        };
        Ok(Self { text })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        {
            let nbt_bytes =
                ferrumc_nbt::write_network_root(&self.text, &ferrumc_nbt::NbtLimits::default())?;
            wire::write_raw(buf, &nbt_bytes);
        }
        Ok(())
    }
}

/// `SetTitleAnimationTimes`: the play clientbound packet (wire id `0x6c`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct SetTitleAnimationTimes {
    fade_in: i32,
    stay: i32,
    fade_out: i32,
}

impl SetTitleAnimationTimes {
    /// The wire packet id for `SetTitleAnimationTimes`.
    pub const PACKET_ID: i32 = 0x6c;

    /// Creates a new `SetTitleAnimationTimes` from its wire fields.
    pub fn new(fade_in: i32, stay: i32, fade_out: i32) -> Self {
        Self {
            fade_in,
            stay,
            fade_out,
        }
    }

    /// Returns the `fade_in` field.
    pub fn fade_in(&self) -> i32 {
        self.fade_in
    }

    /// Returns the `stay` field.
    pub fn stay(&self) -> i32 {
        self.stay
    }

    /// Returns the `fade_out` field.
    pub fn fade_out(&self) -> i32 {
        self.fade_out
    }

    /// Decodes a `SetTitleAnimationTimes` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let fade_in = reader.read_i32()?;
        let stay = reader.read_i32()?;
        let fade_out = reader.read_i32()?;
        Ok(Self {
            fade_in,
            stay,
            fade_out,
        })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        wire::write_i32(buf, self.fade_in);
        wire::write_i32(buf, self.stay);
        wire::write_i32(buf, self.fade_out);
        Ok(())
    }
}

/// `EntitySoundEffect`: the play clientbound packet (wire id `0x6d`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct EntitySoundEffect {
    payload: Vec<u8>,
}

impl EntitySoundEffect {
    /// The wire packet id for `EntitySoundEffect`.
    pub const PACKET_ID: i32 = 0x6d;

    /// Creates a new `EntitySoundEffect` from its wire fields.
    pub fn new(payload: Vec<u8>) -> Self {
        Self { payload }
    }

    /// Returns the `payload` field.
    pub fn payload(&self) -> &[u8] {
        &self.payload
    }

    /// Decodes a `EntitySoundEffect` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let payload = {
            let remaining = reader.remaining();
            reader.read_bytes(remaining)?.to_vec()
        };
        Ok(Self { payload })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        wire::write_raw(buf, &self.payload);
        Ok(())
    }
}

/// `SoundEffect`: the play clientbound packet (wire id `0x6e`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct SoundEffect {
    payload: Vec<u8>,
}

impl SoundEffect {
    /// The wire packet id for `SoundEffect`.
    pub const PACKET_ID: i32 = 0x6e;

    /// Creates a new `SoundEffect` from its wire fields.
    pub fn new(payload: Vec<u8>) -> Self {
        Self { payload }
    }

    /// Returns the `payload` field.
    pub fn payload(&self) -> &[u8] {
        &self.payload
    }

    /// Decodes a `SoundEffect` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let payload = {
            let remaining = reader.remaining();
            reader.read_bytes(remaining)?.to_vec()
        };
        Ok(Self { payload })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        wire::write_raw(buf, &self.payload);
        Ok(())
    }
}

/// `SystemChat`: the play clientbound packet (wire id `0x72`).
#[derive(Debug, Clone, PartialEq)]
pub struct SystemChat {
    content: ferrumc_nbt::NbtTag,
    overlay: bool,
}

impl SystemChat {
    /// The wire packet id for `SystemChat`.
    pub const PACKET_ID: i32 = 0x72;

    /// Creates a new `SystemChat` from its wire fields.
    pub fn new(content: ferrumc_nbt::NbtTag, overlay: bool) -> Self {
        Self { content, overlay }
    }

    /// Returns the `content` field.
    pub fn content(&self) -> &ferrumc_nbt::NbtTag {
        &self.content
    }

    /// Returns the `overlay` field.
    pub fn overlay(&self) -> bool {
        self.overlay
    }

    /// Decodes a `SystemChat` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let content = {
            let available = reader.remaining();
            let bytes = reader.read_bytes(available)?;
            let (tag, consumed) = ferrumc_nbt::read_network_root_with_consumed(
                bytes,
                &ferrumc_nbt::NbtLimits::default(),
            )?;
            *reader = BoundedReader::new(bytes.get(consumed..).unwrap_or_default());
            tag
        };
        let overlay = wire::read_bool(reader)?;
        Ok(Self { content, overlay })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        {
            let nbt_bytes =
                ferrumc_nbt::write_network_root(&self.content, &ferrumc_nbt::NbtLimits::default())?;
            wire::write_raw(buf, &nbt_bytes);
        }
        wire::write_bool(buf, self.overlay);
        Ok(())
    }
}

/// serverbound packets in the play state, dispatched by wire id.
#[derive(Debug, Clone, PartialEq)]
pub enum ServerboundPlayPacket {
    /// The `ConfirmTeleportation` packet.
    ConfirmTeleportation(ConfirmTeleportation),
    /// The `ChatCommand` packet.
    ChatCommand(ChatCommand),
    /// The `ChatMessage` packet.
    ChatMessage(ChatMessage),
    /// The `TabCompleteRequest` packet.
    TabCompleteRequest(TabCompleteRequest),
    /// The `WindowClick` packet.
    WindowClick(WindowClick),
    /// The `ServerboundKeepAlive` packet.
    ServerboundKeepAlive(ServerboundKeepAlive),
    /// The `SetPlayerPosition` packet.
    SetPlayerPosition(SetPlayerPosition),
    /// The `SetPlayerPositionAndRotation` packet.
    SetPlayerPositionAndRotation(SetPlayerPositionAndRotation),
    /// The `SetPlayerRotation` packet.
    SetPlayerRotation(SetPlayerRotation),
    /// The `PlayerAction` packet.
    PlayerAction(PlayerAction),
    /// The `ServerboundSetHeldItem` packet.
    ServerboundSetHeldItem(ServerboundSetHeldItem),
    /// The `SetCreativeSlot` packet.
    SetCreativeSlot(SetCreativeSlot),
    /// The `UpdateSign` packet.
    UpdateSign(UpdateSign),
    /// The `UseItemOn` packet.
    UseItemOn(UseItemOn),
}

impl ServerboundPlayPacket {
    /// Decodes a serverbound play packet body selected by its wire `id`.
    pub fn decode(id: i32, reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        match id {
            ConfirmTeleportation::PACKET_ID => Ok(Self::ConfirmTeleportation(
                ConfirmTeleportation::decode(reader)?,
            )),
            ChatCommand::PACKET_ID => Ok(Self::ChatCommand(ChatCommand::decode(reader)?)),
            ChatMessage::PACKET_ID => Ok(Self::ChatMessage(ChatMessage::decode(reader)?)),
            TabCompleteRequest::PACKET_ID => Ok(Self::TabCompleteRequest(
                TabCompleteRequest::decode(reader)?,
            )),
            WindowClick::PACKET_ID => Ok(Self::WindowClick(WindowClick::decode(reader)?)),
            ServerboundKeepAlive::PACKET_ID => Ok(Self::ServerboundKeepAlive(
                ServerboundKeepAlive::decode(reader)?,
            )),
            SetPlayerPosition::PACKET_ID => {
                Ok(Self::SetPlayerPosition(SetPlayerPosition::decode(reader)?))
            }
            SetPlayerPositionAndRotation::PACKET_ID => Ok(Self::SetPlayerPositionAndRotation(
                SetPlayerPositionAndRotation::decode(reader)?,
            )),
            SetPlayerRotation::PACKET_ID => {
                Ok(Self::SetPlayerRotation(SetPlayerRotation::decode(reader)?))
            }
            PlayerAction::PACKET_ID => Ok(Self::PlayerAction(PlayerAction::decode(reader)?)),
            ServerboundSetHeldItem::PACKET_ID => Ok(Self::ServerboundSetHeldItem(
                ServerboundSetHeldItem::decode(reader)?,
            )),
            SetCreativeSlot::PACKET_ID => {
                Ok(Self::SetCreativeSlot(SetCreativeSlot::decode(reader)?))
            }
            UpdateSign::PACKET_ID => Ok(Self::UpdateSign(UpdateSign::decode(reader)?)),
            UseItemOn::PACKET_ID => Ok(Self::UseItemOn(UseItemOn::decode(reader)?)),
            other => Err(ProtoError::UnknownPacketId {
                state: State::Play,
                direction: Direction::Serverbound,
                id: other,
            }),
        }
    }

    /// Returns the wire packet id of this packet.
    pub fn packet_id(&self) -> i32 {
        match self {
            Self::ConfirmTeleportation(_) => ConfirmTeleportation::PACKET_ID,
            Self::ChatCommand(_) => ChatCommand::PACKET_ID,
            Self::ChatMessage(_) => ChatMessage::PACKET_ID,
            Self::TabCompleteRequest(_) => TabCompleteRequest::PACKET_ID,
            Self::WindowClick(_) => WindowClick::PACKET_ID,
            Self::ServerboundKeepAlive(_) => ServerboundKeepAlive::PACKET_ID,
            Self::SetPlayerPosition(_) => SetPlayerPosition::PACKET_ID,
            Self::SetPlayerPositionAndRotation(_) => SetPlayerPositionAndRotation::PACKET_ID,
            Self::SetPlayerRotation(_) => SetPlayerRotation::PACKET_ID,
            Self::PlayerAction(_) => PlayerAction::PACKET_ID,
            Self::ServerboundSetHeldItem(_) => ServerboundSetHeldItem::PACKET_ID,
            Self::SetCreativeSlot(_) => SetCreativeSlot::PACKET_ID,
            Self::UpdateSign(_) => UpdateSign::PACKET_ID,
            Self::UseItemOn(_) => UseItemOn::PACKET_ID,
        }
    }

    /// Encodes this packet (id followed by its body) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        match self {
            Self::ConfirmTeleportation(packet) => packet.encode(buf),
            Self::ChatCommand(packet) => packet.encode(buf),
            Self::ChatMessage(packet) => packet.encode(buf),
            Self::TabCompleteRequest(packet) => packet.encode(buf),
            Self::WindowClick(packet) => packet.encode(buf),
            Self::ServerboundKeepAlive(packet) => packet.encode(buf),
            Self::SetPlayerPosition(packet) => packet.encode(buf),
            Self::SetPlayerPositionAndRotation(packet) => packet.encode(buf),
            Self::SetPlayerRotation(packet) => packet.encode(buf),
            Self::PlayerAction(packet) => packet.encode(buf),
            Self::ServerboundSetHeldItem(packet) => packet.encode(buf),
            Self::SetCreativeSlot(packet) => packet.encode(buf),
            Self::UpdateSign(packet) => packet.encode(buf),
            Self::UseItemOn(packet) => packet.encode(buf),
        }
    }
}

/// clientbound packets in the play state, dispatched by wire id.
#[derive(Debug, Clone, PartialEq)]
pub enum ClientboundPlayPacket {
    /// The `SpawnEntity` packet.
    SpawnEntity(SpawnEntity),
    /// The `AcknowledgeBlockChange` packet.
    AcknowledgeBlockChange(AcknowledgeBlockChange),
    /// The `BlockEntityData` packet.
    BlockEntityData(BlockEntityData),
    /// The `BlockUpdate` packet.
    BlockUpdate(BlockUpdate),
    /// The `BossBar` packet.
    BossBar(BossBar),
    /// The `TabCompleteResponse` packet.
    TabCompleteResponse(TabCompleteResponse),
    /// The `Commands` packet.
    Commands(Commands),
    /// The `SetContainerContent` packet.
    SetContainerContent(SetContainerContent),
    /// The `SetContainerSlot` packet.
    SetContainerSlot(SetContainerSlot),
    /// The `EntityTeleport` packet.
    EntityTeleport(EntityTeleport),
    /// The `UnloadChunk` packet.
    UnloadChunk(UnloadChunk),
    /// The `GameEvent` packet.
    GameEvent(GameEvent),
    /// The `ClientboundKeepAlive` packet.
    ClientboundKeepAlive(ClientboundKeepAlive),
    /// The `ChunkDataAndLight` packet.
    ChunkDataAndLight(ChunkDataAndLight),
    /// The `Particle` packet.
    Particle(Particle),
    /// The `JoinGame` packet.
    JoinGame(JoinGame),
    /// The `UpdateEntityPosition` packet.
    UpdateEntityPosition(UpdateEntityPosition),
    /// The `UpdateEntityPositionAndRotation` packet.
    UpdateEntityPositionAndRotation(UpdateEntityPositionAndRotation),
    /// The `UpdateEntityRotation` packet.
    UpdateEntityRotation(UpdateEntityRotation),
    /// The `OpenSignEditor` packet.
    OpenSignEditor(OpenSignEditor),
    /// The `PlayerAbilities` packet.
    PlayerAbilities(PlayerAbilities),
    /// The `RemovePlayerInfo` packet.
    RemovePlayerInfo(RemovePlayerInfo),
    /// The `PlayerInfoUpdate` packet.
    PlayerInfoUpdate(PlayerInfoUpdate),
    /// The `SynchronizePlayerPosition` packet.
    SynchronizePlayerPosition(SynchronizePlayerPosition),
    /// The `RemoveEntities` packet.
    RemoveEntities(RemoveEntities),
    /// The `SetHeadRotation` packet.
    SetHeadRotation(SetHeadRotation),
    /// The `SetActionBarText` packet.
    SetActionBarText(SetActionBarText),
    /// The `SetCenterChunk` packet.
    SetCenterChunk(SetCenterChunk),
    /// The `SetDefaultSpawnPosition` packet.
    SetDefaultSpawnPosition(SetDefaultSpawnPosition),
    /// The `DisplayObjective` packet.
    DisplayObjective(DisplayObjective),
    /// The `SetEquipment` packet.
    SetEquipment(SetEquipment),
    /// The `ClientboundSetHeldItem` packet.
    ClientboundSetHeldItem(ClientboundSetHeldItem),
    /// The `UpdateObjectives` packet.
    UpdateObjectives(UpdateObjectives),
    /// The `SetPlayerTeam` packet.
    SetPlayerTeam(SetPlayerTeam),
    /// The `UpdateScore` packet.
    UpdateScore(UpdateScore),
    /// The `SetSubtitleText` packet.
    SetSubtitleText(SetSubtitleText),
    /// The `SetTitleText` packet.
    SetTitleText(SetTitleText),
    /// The `SetTitleAnimationTimes` packet.
    SetTitleAnimationTimes(SetTitleAnimationTimes),
    /// The `EntitySoundEffect` packet.
    EntitySoundEffect(EntitySoundEffect),
    /// The `SoundEffect` packet.
    SoundEffect(SoundEffect),
    /// The `SystemChat` packet.
    SystemChat(SystemChat),
}

impl ClientboundPlayPacket {
    /// Decodes a clientbound play packet body selected by its wire `id`.
    pub fn decode(id: i32, reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        match id {
            SpawnEntity::PACKET_ID => Ok(Self::SpawnEntity(SpawnEntity::decode(reader)?)),
            AcknowledgeBlockChange::PACKET_ID => Ok(Self::AcknowledgeBlockChange(
                AcknowledgeBlockChange::decode(reader)?,
            )),
            BlockEntityData::PACKET_ID => {
                Ok(Self::BlockEntityData(BlockEntityData::decode(reader)?))
            }
            BlockUpdate::PACKET_ID => Ok(Self::BlockUpdate(BlockUpdate::decode(reader)?)),
            BossBar::PACKET_ID => Ok(Self::BossBar(BossBar::decode(reader)?)),
            TabCompleteResponse::PACKET_ID => Ok(Self::TabCompleteResponse(
                TabCompleteResponse::decode(reader)?,
            )),
            Commands::PACKET_ID => Ok(Self::Commands(Commands::decode(reader)?)),
            SetContainerContent::PACKET_ID => Ok(Self::SetContainerContent(
                SetContainerContent::decode(reader)?,
            )),
            SetContainerSlot::PACKET_ID => {
                Ok(Self::SetContainerSlot(SetContainerSlot::decode(reader)?))
            }
            EntityTeleport::PACKET_ID => Ok(Self::EntityTeleport(EntityTeleport::decode(reader)?)),
            UnloadChunk::PACKET_ID => Ok(Self::UnloadChunk(UnloadChunk::decode(reader)?)),
            GameEvent::PACKET_ID => Ok(Self::GameEvent(GameEvent::decode(reader)?)),
            ClientboundKeepAlive::PACKET_ID => Ok(Self::ClientboundKeepAlive(
                ClientboundKeepAlive::decode(reader)?,
            )),
            ChunkDataAndLight::PACKET_ID => {
                Ok(Self::ChunkDataAndLight(ChunkDataAndLight::decode(reader)?))
            }
            Particle::PACKET_ID => Ok(Self::Particle(Particle::decode(reader)?)),
            JoinGame::PACKET_ID => Ok(Self::JoinGame(JoinGame::decode(reader)?)),
            UpdateEntityPosition::PACKET_ID => Ok(Self::UpdateEntityPosition(
                UpdateEntityPosition::decode(reader)?,
            )),
            UpdateEntityPositionAndRotation::PACKET_ID => {
                Ok(Self::UpdateEntityPositionAndRotation(
                    UpdateEntityPositionAndRotation::decode(reader)?,
                ))
            }
            UpdateEntityRotation::PACKET_ID => Ok(Self::UpdateEntityRotation(
                UpdateEntityRotation::decode(reader)?,
            )),
            OpenSignEditor::PACKET_ID => Ok(Self::OpenSignEditor(OpenSignEditor::decode(reader)?)),
            PlayerAbilities::PACKET_ID => {
                Ok(Self::PlayerAbilities(PlayerAbilities::decode(reader)?))
            }
            RemovePlayerInfo::PACKET_ID => {
                Ok(Self::RemovePlayerInfo(RemovePlayerInfo::decode(reader)?))
            }
            PlayerInfoUpdate::PACKET_ID => {
                Ok(Self::PlayerInfoUpdate(PlayerInfoUpdate::decode(reader)?))
            }
            SynchronizePlayerPosition::PACKET_ID => Ok(Self::SynchronizePlayerPosition(
                SynchronizePlayerPosition::decode(reader)?,
            )),
            RemoveEntities::PACKET_ID => Ok(Self::RemoveEntities(RemoveEntities::decode(reader)?)),
            SetHeadRotation::PACKET_ID => {
                Ok(Self::SetHeadRotation(SetHeadRotation::decode(reader)?))
            }
            SetActionBarText::PACKET_ID => {
                Ok(Self::SetActionBarText(SetActionBarText::decode(reader)?))
            }
            SetCenterChunk::PACKET_ID => Ok(Self::SetCenterChunk(SetCenterChunk::decode(reader)?)),
            SetDefaultSpawnPosition::PACKET_ID => Ok(Self::SetDefaultSpawnPosition(
                SetDefaultSpawnPosition::decode(reader)?,
            )),
            DisplayObjective::PACKET_ID => {
                Ok(Self::DisplayObjective(DisplayObjective::decode(reader)?))
            }
            SetEquipment::PACKET_ID => Ok(Self::SetEquipment(SetEquipment::decode(reader)?)),
            ClientboundSetHeldItem::PACKET_ID => Ok(Self::ClientboundSetHeldItem(
                ClientboundSetHeldItem::decode(reader)?,
            )),
            UpdateObjectives::PACKET_ID => {
                Ok(Self::UpdateObjectives(UpdateObjectives::decode(reader)?))
            }
            SetPlayerTeam::PACKET_ID => Ok(Self::SetPlayerTeam(SetPlayerTeam::decode(reader)?)),
            UpdateScore::PACKET_ID => Ok(Self::UpdateScore(UpdateScore::decode(reader)?)),
            SetSubtitleText::PACKET_ID => {
                Ok(Self::SetSubtitleText(SetSubtitleText::decode(reader)?))
            }
            SetTitleText::PACKET_ID => Ok(Self::SetTitleText(SetTitleText::decode(reader)?)),
            SetTitleAnimationTimes::PACKET_ID => Ok(Self::SetTitleAnimationTimes(
                SetTitleAnimationTimes::decode(reader)?,
            )),
            EntitySoundEffect::PACKET_ID => {
                Ok(Self::EntitySoundEffect(EntitySoundEffect::decode(reader)?))
            }
            SoundEffect::PACKET_ID => Ok(Self::SoundEffect(SoundEffect::decode(reader)?)),
            SystemChat::PACKET_ID => Ok(Self::SystemChat(SystemChat::decode(reader)?)),
            other => Err(ProtoError::UnknownPacketId {
                state: State::Play,
                direction: Direction::Clientbound,
                id: other,
            }),
        }
    }

    /// Returns the wire packet id of this packet.
    pub fn packet_id(&self) -> i32 {
        match self {
            Self::SpawnEntity(_) => SpawnEntity::PACKET_ID,
            Self::AcknowledgeBlockChange(_) => AcknowledgeBlockChange::PACKET_ID,
            Self::BlockEntityData(_) => BlockEntityData::PACKET_ID,
            Self::BlockUpdate(_) => BlockUpdate::PACKET_ID,
            Self::BossBar(_) => BossBar::PACKET_ID,
            Self::TabCompleteResponse(_) => TabCompleteResponse::PACKET_ID,
            Self::Commands(_) => Commands::PACKET_ID,
            Self::SetContainerContent(_) => SetContainerContent::PACKET_ID,
            Self::SetContainerSlot(_) => SetContainerSlot::PACKET_ID,
            Self::EntityTeleport(_) => EntityTeleport::PACKET_ID,
            Self::UnloadChunk(_) => UnloadChunk::PACKET_ID,
            Self::GameEvent(_) => GameEvent::PACKET_ID,
            Self::ClientboundKeepAlive(_) => ClientboundKeepAlive::PACKET_ID,
            Self::ChunkDataAndLight(_) => ChunkDataAndLight::PACKET_ID,
            Self::Particle(_) => Particle::PACKET_ID,
            Self::JoinGame(_) => JoinGame::PACKET_ID,
            Self::UpdateEntityPosition(_) => UpdateEntityPosition::PACKET_ID,
            Self::UpdateEntityPositionAndRotation(_) => UpdateEntityPositionAndRotation::PACKET_ID,
            Self::UpdateEntityRotation(_) => UpdateEntityRotation::PACKET_ID,
            Self::OpenSignEditor(_) => OpenSignEditor::PACKET_ID,
            Self::PlayerAbilities(_) => PlayerAbilities::PACKET_ID,
            Self::RemovePlayerInfo(_) => RemovePlayerInfo::PACKET_ID,
            Self::PlayerInfoUpdate(_) => PlayerInfoUpdate::PACKET_ID,
            Self::SynchronizePlayerPosition(_) => SynchronizePlayerPosition::PACKET_ID,
            Self::RemoveEntities(_) => RemoveEntities::PACKET_ID,
            Self::SetHeadRotation(_) => SetHeadRotation::PACKET_ID,
            Self::SetActionBarText(_) => SetActionBarText::PACKET_ID,
            Self::SetCenterChunk(_) => SetCenterChunk::PACKET_ID,
            Self::SetDefaultSpawnPosition(_) => SetDefaultSpawnPosition::PACKET_ID,
            Self::DisplayObjective(_) => DisplayObjective::PACKET_ID,
            Self::SetEquipment(_) => SetEquipment::PACKET_ID,
            Self::ClientboundSetHeldItem(_) => ClientboundSetHeldItem::PACKET_ID,
            Self::UpdateObjectives(_) => UpdateObjectives::PACKET_ID,
            Self::SetPlayerTeam(_) => SetPlayerTeam::PACKET_ID,
            Self::UpdateScore(_) => UpdateScore::PACKET_ID,
            Self::SetSubtitleText(_) => SetSubtitleText::PACKET_ID,
            Self::SetTitleText(_) => SetTitleText::PACKET_ID,
            Self::SetTitleAnimationTimes(_) => SetTitleAnimationTimes::PACKET_ID,
            Self::EntitySoundEffect(_) => EntitySoundEffect::PACKET_ID,
            Self::SoundEffect(_) => SoundEffect::PACKET_ID,
            Self::SystemChat(_) => SystemChat::PACKET_ID,
        }
    }

    /// Encodes this packet (id followed by its body) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        match self {
            Self::SpawnEntity(packet) => packet.encode(buf),
            Self::AcknowledgeBlockChange(packet) => packet.encode(buf),
            Self::BlockEntityData(packet) => packet.encode(buf),
            Self::BlockUpdate(packet) => packet.encode(buf),
            Self::BossBar(packet) => packet.encode(buf),
            Self::TabCompleteResponse(packet) => packet.encode(buf),
            Self::Commands(packet) => packet.encode(buf),
            Self::SetContainerContent(packet) => packet.encode(buf),
            Self::SetContainerSlot(packet) => packet.encode(buf),
            Self::EntityTeleport(packet) => packet.encode(buf),
            Self::UnloadChunk(packet) => packet.encode(buf),
            Self::GameEvent(packet) => packet.encode(buf),
            Self::ClientboundKeepAlive(packet) => packet.encode(buf),
            Self::ChunkDataAndLight(packet) => packet.encode(buf),
            Self::Particle(packet) => packet.encode(buf),
            Self::JoinGame(packet) => packet.encode(buf),
            Self::UpdateEntityPosition(packet) => packet.encode(buf),
            Self::UpdateEntityPositionAndRotation(packet) => packet.encode(buf),
            Self::UpdateEntityRotation(packet) => packet.encode(buf),
            Self::OpenSignEditor(packet) => packet.encode(buf),
            Self::PlayerAbilities(packet) => packet.encode(buf),
            Self::RemovePlayerInfo(packet) => packet.encode(buf),
            Self::PlayerInfoUpdate(packet) => packet.encode(buf),
            Self::SynchronizePlayerPosition(packet) => packet.encode(buf),
            Self::RemoveEntities(packet) => packet.encode(buf),
            Self::SetHeadRotation(packet) => packet.encode(buf),
            Self::SetActionBarText(packet) => packet.encode(buf),
            Self::SetCenterChunk(packet) => packet.encode(buf),
            Self::SetDefaultSpawnPosition(packet) => packet.encode(buf),
            Self::DisplayObjective(packet) => packet.encode(buf),
            Self::SetEquipment(packet) => packet.encode(buf),
            Self::ClientboundSetHeldItem(packet) => packet.encode(buf),
            Self::UpdateObjectives(packet) => packet.encode(buf),
            Self::SetPlayerTeam(packet) => packet.encode(buf),
            Self::UpdateScore(packet) => packet.encode(buf),
            Self::SetSubtitleText(packet) => packet.encode(buf),
            Self::SetTitleText(packet) => packet.encode(buf),
            Self::SetTitleAnimationTimes(packet) => packet.encode(buf),
            Self::EntitySoundEffect(packet) => packet.encode(buf),
            Self::SoundEffect(packet) => packet.encode(buf),
            Self::SystemChat(packet) => packet.encode(buf),
        }
    }
}
