// @generated by ferrumc-proto-gen. Do not edit by hand.
// source: minecraft-data a8cf733fe44f3069f87f63e7ec3d74b521840ded (Minecraft 1.21.8, protocol 772, data version 4440)

//! Packets for the status state (Minecraft 1.21.8, protocol 772).

use bytes::BytesMut;

use ferrumc_codec::{BoundedReader, BoundedString};

use crate::error::ProtoError;
use crate::wire;
use crate::{Direction, State};

/// `StatusRequest`: the status serverbound packet (wire id `0x00`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct StatusRequest;

impl StatusRequest {
    /// The wire packet id for `StatusRequest`.
    pub const PACKET_ID: i32 = 0x00;

    /// Decodes a `StatusRequest` body from `reader` (any packet id is already consumed).
    pub fn decode(_reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        Ok(Self)
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        Ok(())
    }
}

/// `PingRequest`: the status serverbound packet (wire id `0x01`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct PingRequest {
    payload: i64,
}

impl PingRequest {
    /// The wire packet id for `PingRequest`.
    pub const PACKET_ID: i32 = 0x01;

    /// Creates a new `PingRequest` from its wire fields.
    pub fn new(payload: i64) -> Self {
        Self { payload }
    }

    /// Returns the `payload` field.
    pub fn payload(&self) -> i64 {
        self.payload
    }

    /// Decodes a `PingRequest` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let payload = reader.read_i64()?;
        Ok(Self { payload })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        wire::write_i64(buf, self.payload);
        Ok(())
    }
}

/// `StatusResponse`: the status clientbound packet (wire id `0x00`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct StatusResponse {
    json: BoundedString<32_767>,
}

impl StatusResponse {
    /// The wire packet id for `StatusResponse`.
    pub const PACKET_ID: i32 = 0x00;

    /// Creates a new `StatusResponse` from its wire fields.
    pub fn new(json: BoundedString<32_767>) -> Self {
        Self { json }
    }

    /// Returns the `json` field.
    pub fn json(&self) -> &BoundedString<32_767> {
        &self.json
    }

    /// Decodes a `StatusResponse` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let json = BoundedString::<32_767>::read(reader)?;
        Ok(Self { json })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        self.json.write(buf);
        Ok(())
    }
}

/// `PongResponse`: the status clientbound packet (wire id `0x01`).
#[derive(Debug, Clone, PartialEq, Eq)]
pub struct PongResponse {
    payload: i64,
}

impl PongResponse {
    /// The wire packet id for `PongResponse`.
    pub const PACKET_ID: i32 = 0x01;

    /// Creates a new `PongResponse` from its wire fields.
    pub fn new(payload: i64) -> Self {
        Self { payload }
    }

    /// Returns the `payload` field.
    pub fn payload(&self) -> i64 {
        self.payload
    }

    /// Decodes a `PongResponse` body from `reader` (any packet id is already consumed).
    pub fn decode(reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        let payload = reader.read_i64()?;
        Ok(Self { payload })
    }

    /// Encodes this value (packet id, when present, then fields) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        ferrumc_codec::write_var_int(buf, Self::PACKET_ID);
        wire::write_i64(buf, self.payload);
        Ok(())
    }
}

/// serverbound packets in the status state, dispatched by wire id.
#[derive(Debug, Clone, PartialEq, Eq)]
pub enum ServerboundStatusPacket {
    /// The `StatusRequest` packet.
    StatusRequest(StatusRequest),
    /// The `PingRequest` packet.
    PingRequest(PingRequest),
}

impl ServerboundStatusPacket {
    /// Decodes a serverbound status packet body selected by its wire `id`.
    pub fn decode(id: i32, reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        match id {
            StatusRequest::PACKET_ID => Ok(Self::StatusRequest(StatusRequest::decode(reader)?)),
            PingRequest::PACKET_ID => Ok(Self::PingRequest(PingRequest::decode(reader)?)),
            other => Err(ProtoError::UnknownPacketId {
                state: State::Status,
                direction: Direction::Serverbound,
                id: other,
            }),
        }
    }

    /// Returns the wire packet id of this packet.
    pub fn packet_id(&self) -> i32 {
        match self {
            Self::StatusRequest(_) => StatusRequest::PACKET_ID,
            Self::PingRequest(_) => PingRequest::PACKET_ID,
        }
    }

    /// Encodes this packet (id followed by its body) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        match self {
            Self::StatusRequest(packet) => packet.encode(buf),
            Self::PingRequest(packet) => packet.encode(buf),
        }
    }
}

/// clientbound packets in the status state, dispatched by wire id.
#[derive(Debug, Clone, PartialEq, Eq)]
pub enum ClientboundStatusPacket {
    /// The `StatusResponse` packet.
    StatusResponse(StatusResponse),
    /// The `PongResponse` packet.
    PongResponse(PongResponse),
}

impl ClientboundStatusPacket {
    /// Decodes a clientbound status packet body selected by its wire `id`.
    pub fn decode(id: i32, reader: &mut BoundedReader<'_>) -> Result<Self, ProtoError> {
        match id {
            StatusResponse::PACKET_ID => Ok(Self::StatusResponse(StatusResponse::decode(reader)?)),
            PongResponse::PACKET_ID => Ok(Self::PongResponse(PongResponse::decode(reader)?)),
            other => Err(ProtoError::UnknownPacketId {
                state: State::Status,
                direction: Direction::Clientbound,
                id: other,
            }),
        }
    }

    /// Returns the wire packet id of this packet.
    pub fn packet_id(&self) -> i32 {
        match self {
            Self::StatusResponse(_) => StatusResponse::PACKET_ID,
            Self::PongResponse(_) => PongResponse::PACKET_ID,
        }
    }

    /// Encodes this packet (id followed by its body) into `buf`.
    pub fn encode(&self, buf: &mut BytesMut) -> Result<(), ProtoError> {
        match self {
            Self::StatusResponse(packet) => packet.encode(buf),
            Self::PongResponse(packet) => packet.encode(buf),
        }
    }
}
