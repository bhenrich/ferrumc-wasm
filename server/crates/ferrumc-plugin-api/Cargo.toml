[package]
name = "ferrumc-plugin-api"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
ferrumc-core = { workspace = true }
ferrumc-math = { workspace = true }
ferrumc-command = { workspace = true }
ferrumc-permission = { workspace = true }
# Classifying error types for the plugin-facing API surface.
thiserror = { workspace = true }
# Plugin versions follow semantic versioning; reuse the workspace semver crate
# rather than hand-rolling a version type.
semver = { workspace = true }

[dev-dependencies]

[lints]
workspace = true
