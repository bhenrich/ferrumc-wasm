// Pure client-rendered SPA: the dashboard is a live console with no static
// content to prerender, and it only ever runs against a loopback server.
export const ssr = false;
export const prerender = false;
