#![forbid(unsafe_code)]

//! Read-only localhost observability dashboard (axum + htmx). Filled by the dashboard workstream.

// placeholder

#[cfg(test)]
mod tests {
    #[test]
    fn crate_compiles() {}
}
