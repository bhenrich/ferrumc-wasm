#![forbid(unsafe_code)]

//! Pure block-placement rules (rotation/facing/half) for `FerrumC` v2. Filled by the placement workstream.

// placeholder

#[cfg(test)]
mod tests {
    #[test]
    fn crate_compiles() {}
}
