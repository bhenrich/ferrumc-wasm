[package]
name = "ferrumc-math"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
# Mandated shared-types dependency (ferrumc-math depends on ferrumc-core per the
# crate map). The coordinate/geometry types are self-contained, but `WorldIntent`
# (intent.rs) uses core's `PlayerId` / `TextComponent`, so the dependency is live.
ferrumc-core = { workspace = true }

[dev-dependencies]

[lints]
workspace = true
