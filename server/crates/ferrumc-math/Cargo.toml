[package]
name = "ferrumc-math"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
# Mandated shared-types dependency (ferrumc-math depends on ferrumc-core per the
# crate map). The coordinate/geometry types here are self-contained value types
# and need none of core's types yet, so lib.rs binds the crate anonymously to
# keep the link intentional rather than dead weight.
ferrumc-core = { workspace = true }

[dev-dependencies]

[lints]
workspace = true
